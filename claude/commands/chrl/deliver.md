---
description: "ChangeRail deliver shorthand: alias for /changerail:deliver"
argument-hint: <card-or-column-path> [--max-cards N] [--no-push] [--max-review-cycles N]
---

Run the **`chrl-deliver`** skill alias.

Load and follow `chrl-deliver` through Claude skill discovery. It delegates to
the canonical `changerail-deliver` contract, so this `/chrl:deliver` command is
the daily shorthand for `/changerail:deliver`.

Arguments: $ARGUMENTS
