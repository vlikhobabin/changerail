---
description: "ChangeRail fast-forward shorthand: alias for /changerail:ff"
argument-hint: <card-path> [--max-changes N] [--from slug] [--until slug] [--no-move]
---

Run the **`chrl-ff`** skill alias.

Load and follow `chrl-ff` through Claude skill discovery. It delegates to the
canonical `changerail-ff` contract, so this `/chrl:ff` command is the daily
shorthand for `/changerail:ff`.

Arguments: $ARGUMENTS
