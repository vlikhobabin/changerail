---
description: "ChangeRail pub shorthand: alias for /changerail:pub"
argument-hint: <card-path> [--no-push] [--message text] [--docs-only]
---

Run the **`chrl-pub`** skill alias.

Load and follow `chrl-pub` through Claude skill discovery. It delegates to the
canonical `changerail-pub` contract, so this `/chrl:pub` command is the daily
shorthand for `/changerail:pub`.

Arguments: $ARGUMENTS
