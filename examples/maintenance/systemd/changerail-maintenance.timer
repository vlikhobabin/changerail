[Unit]
Description=Run ChangeRail read-only maintenance audit daily

[Timer]
OnCalendar=daily
RandomizedDelaySec=30m
Persistent=true

[Install]
WantedBy=timers.target

# systemd timers are scheduler triggers, not exactly-once guarantees. The
# ChangeRail runner still performs repository-local non-overlap checks.
