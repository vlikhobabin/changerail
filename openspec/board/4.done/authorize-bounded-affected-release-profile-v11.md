# Авторизовать bounded affected release profile v11

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
archived

## Series
- release-baseline-acceleration

## Series Index
- 29

## Source
- Published runtime-root and scheduler-matrix investigation
  `investigate-affected-release-profile-runtime-root-scheduler-matrix-v11`,
  exact commit `c1df80ff4591c6c2619856b91cc4e1bcdc50cec6`.
- Published integration decision, semantic scheduler v1, affected v10
  authorization and accumulated `changerail-release-ci` contract.
- Terminal unpublished affected v10 implementation and all of its card, code,
  tests, specs, manifest, verdicts, logs and raw evidence remain forensic-only;
  they are not read, copied, cherry-picked or accepted.

## Summary
Авторизовать ровно один clean implementation successor для affected profile
v11 с exact dedicated runtime-root admission, read-only task-root
pre-reservation, independently complete scheduler matrix и `<=499` production
LOC.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`
- Independent review: one fresh `gpt-5.6-sol`/`high`
- Same-card repair budget limit/used/remaining: `1/0/1`, exhausted `false`

## Depends On
- `investigate-affected-release-profile-runtime-root-scheduler-matrix-v11`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`
- `authorize-bounded-affected-release-profile-v10`

## Blocks
- `implement-bounded-affected-release-profile-v11`

## Authorization
- Investigation authorization:
  `{"investigation_card":"openspec/board/4.done/investigate-affected-release-profile-runtime-root-scheduler-matrix-v11.md","investigation_id":"investigate-affected-release-profile-runtime-root-scheduler-matrix-v11","successor_card":"openspec/board/3.inprogress/implement-bounded-affected-release-profile-v11.md","successor_id":"implement-bounded-affected-release-profile-v11","production_loc_ceiling":500,"allow_new_authority_or_wire_protocol":true}`

## Acceptance
- Authorization публикуется docs-only от exact investigation HEAD
  `c1df80ff...`; remote investigation и authorization branches до mutation
  указывают ровно на этот SHA, а six-field object выше не имеет дополнительных
  полей или wrappers.
- `Depends On` содержит ровно investigation v11, integration decision,
  scheduler v1 implementation и affected v10 authorization; `Blocks` содержит
  только exact implementation v11.
- Future implementation использует только
  `{"authorization_card":"openspec/board/4.done/authorize-bounded-affected-release-profile-v11.md","authorization_id":"authorize-bounded-affected-release-profile-v11"}`,
  начинается от authorization-publishing HEAD, добавляет не более 499
  production LOC, зависит ровно от investigation v11, integration decision,
  scheduler v1, authorization v10 и этой authorization и блокирует только
  `certify-accelerated-release-loop-v1`.
- До первой production, CI или main-spec mutation future v11 содержит только
  implementation card, same-slug OpenSpec и focused-test artifacts и напрямую
  запускает real failing focused test через `bin/changerail-evidence capture`.
- Captured RED command сначала печатает
  `bin/changerail-review-verdict fingerprint --workspace .`, затем сохраняет
  настоящий non-zero focused-test exit; retained entry содержит `failed`,
  non-zero exit, `tree_sha`, `diff_fingerprint` и конкретную missing production
  module/symbol ошибку, saved tree существует до production mutation и
  независимо реконструируется относительно authorization HEAD.
- Runtime-root admission future v11 принимает только exact frozen dedicated
  repository-relative target: bounded direct-child ASCII leaf; отклоняет empty,
  `.`, `..`, repository root, NUL, surrogate/non-encodable, overlong,
  absolute/multi-component/alternate leaf, lexical/real escape, symlinked
  ancestor/leaf, wrong type/access и non-dedicated target до Git, `run_plan`
  или filesystem mutation.
- Future v11 read-only проверяет каждый выбранный task root до reservation:
  exact bounded unique direct-child token, lexical/real containment и
  отсутствие existing leaf/symlink/conflict; один aggregate fault даёт
  `semantic_started: 0`, а race/reservation failure остаётся bounded,
  non-authoritative и очищает только созданные этой попыткой roots.
- Scheduler proof future v11 имеет независимые normative requirement-to-row map
  и immutable reason schema, перечисляет все `completed`, terminal, outer,
  synthetic и `cancelled` tuples, top-level version/jobs/status/size,
  identity/order/count и exact valid boundary neighbors плюс one-field invalid
  type/bound/cross-field cases для каждого row field.
- Executable catalog future v11 обязан совпадать с independent required-row set
  в обе стороны; missing/extra/duplicate/reused row, masked/no-op/disconnected
  mutant, непроверенный valid tuple и accepted one-field mutant fail closed
  через public `profile.main`/`run_smoke` без replacement production functions.
- Future v11 сохраняет exact 35-ID digest и 35→30 typed ownership, aggregate
  pre-mutation admission, effective `purelib`/`platlib` origins, strict
  committed/staged/unstaged/untracked NUL selection, scheduler-v1 sole
  activation, full-only authority, exact source-safe four-step CI, connected
  resolved-base guards и protocol-artifact non-authority.
- Terminal unpublished v10 payload/runtime evidence не читаются, не копируются,
  не cherry-pick-ятся и не принимаются; future v11 реконструируется только из
  published contracts после remote publication этой authorization.
- Authorization добавляет production/test/runtime LOC `0`, изменяет только
  свою card, same-slug OpenSpec artifacts, synchronized release-CI spec и
  archive metadata; implementation v11 и certification остаются отсутствующими.
- History, real full baseline, affected execution/benchmark, live matrix и
  certification checks не запускаются и не принимаются.

## Change Set
- `authorize-bounded-affected-release-profile-v11`

## Verify
- GREEN required: exact source/remote/object/dependencies/sole block,
  implementation absence, retained RED boundary, dedicated runtime-root and
  task pre-reservation admission, independent exhaustive scheduler matrix,
  accumulated affected floor, strict OpenSpec, JSON/TOML, source
  classification, current-only public scan, archive/main sync, whitespace,
  manifest scope and ordinary/high preflight.
- RED: not applicable; docs-only authorization adds no executable behavior.
- Prohibited: history, full baseline, affected execution/benchmark, live
  matrix, implementation creation, certification and terminal v10 reuse.
- GREEN: exact base/remote, six-field object, dependency/block, two-field
  successor, retained RED, investigated-boundary and absence oracles passed;
  strict OpenSpec passed `23/23` after archive; JSON/TOML parsing, source
  classification, current-only public scan `1601/0`, archive/main exact sync
  and whitespace checks passed with production/test/runtime LOC `0`.
- NOT RUN by contract: reachable history, real full/affected execution or
  benchmark, live matrix and certification checks.

## Archive
- `openspec/changes/archive/2026-08-28-authorize-bounded-affected-release-profile-v11/`

## Related
- `openspec/changes/archive/2026-08-28-authorize-bounded-affected-release-profile-v11/`
- `openspec/board/4.done/investigate-affected-release-profile-runtime-root-scheduler-matrix-v11.md`
- `openspec/board/4.done/authorize-bounded-affected-release-profile-v10.md`
- `openspec/specs/changerail-release-ci/spec.md`

## Result
DO complete: one exact docs-only v11 authorization is synchronized and
archived. Implementation successor, focused tests and executable payload remain
absent; awaiting fresh ordinary/high review before publish.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `authorize-bounded-affected-release-profile-v11`

### Why
Published investigation v11 requires a separately reviewed and remotely
published docs-only authorization before any v11 implementation card, focused
test or executable work may exist.

### Goal
Publish one exact docs-only authorization for the sole clean v11 implementation.

### Scope
- this card;
- same-slug OpenSpec artifacts;
- synchronized `changerail-release-ci` specification and archive metadata.

### Acceptance
- Exact source object, direct investigation dependency, future reference, sole
  block, retained RED, dedicated runtime-root/pre-reservation, independent
  scheduler matrix, accumulated floor, LOC and dormancy contracts are
  machine-checkable.

### Depends On
- `investigate-affected-release-profile-runtime-root-scheduler-matrix-v11`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`
- `authorize-bounded-affected-release-profile-v10`

### Related
- `openspec/changes/authorize-bounded-affected-release-profile-v11/`

## Log
- 2026-08-28 created in a clean worktree and remote branch from exact published
  investigation v11 HEAD; terminal v10 forensic payload/evidence were not read
  or imported, and implementation v11 remains absent.
- 2026-08-28 FF created one apply-ready same-slug docs-only authorization with
  exact object/dependencies, retained RED, dedicated runtime-root/task-root and
  independent scheduler-matrix contracts; strict target/all and whitespace
  checks passed while v11 executable successor and prohibited evidence remained
  absent.
- 2026-08-28 DO synchronized four release-CI requirements, archived the
  same-slug change and passed exact object/dependency/absence, strict OpenSpec,
  JSON/TOML, source-classification, current-only public-surface and whitespace
  checks with production/test/runtime LOC `0`; no prohibited release or
  certification execution ran.
- 2026-08-28T08:46:23Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.
