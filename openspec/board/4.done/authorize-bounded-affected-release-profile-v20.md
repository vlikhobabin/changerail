# Авторизовать bounded affected release profile v20

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
archived

## Series
- release-baseline-acceleration

## Series Index
- 53

## Source
- Published admission/hosted/activation-closure investigation
  `investigate-affected-release-profile-admission-hosted-activation-closure-v20`,
  exact commit `eba34c3ef965a8c4f6ffa68261408101049399c5`.
- Published authorization v19, immutable authorization v18 proof inventory,
  integration decision, semantic scheduler v1 and accumulated
  `changerail-release-ci` contract.
- Terminal unpublished implementation v19 card, OpenSpec payload, source,
  tests, CI/main-spec mutation, manifest, verdicts, logs and raw evidence
  remain forensic-only and are not read, copied, cherry-picked or accepted.

## Summary
Опубликовать одну clean docs-only authorization v20, которая связывает sole
implementation successor с process-free aggregate admission barrier,
component-safe CI-derived hosted targets и единой source-derived activation
worklist/projection при неизменном published v18 proof inventory.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`
- Independent review: one fresh `gpt-5.6-sol`/`high`
- Same-card repair budget limit/used/remaining: `1/0/1`, exhausted `false`

## Depends On
- `investigate-affected-release-profile-admission-hosted-activation-closure-v20`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`
- `authorize-bounded-affected-release-profile-v19`

## Blocks
- `implement-bounded-affected-release-profile-v20`

## Authorization
- Investigation authorization:
  `{"investigation_card":"openspec/board/4.done/investigate-affected-release-profile-admission-hosted-activation-closure-v20.md","investigation_id":"investigate-affected-release-profile-admission-hosted-activation-closure-v20","successor_card":"openspec/board/3.inprogress/implement-bounded-affected-release-profile-v20.md","successor_id":"implement-bounded-affected-release-profile-v20","production_loc_ceiling":500,"allow_new_authority_or_wire_protocol":true}`

## Acceptance
- Authorization публикуется docs-only от exact investigation v20 HEAD
  `eba34c3ef965a8c4f6ffa68261408101049399c5`; local/upstream/remote
  authorization branch и remote investigation branch до mutation указывают на
  этот SHA, а six-field object не имеет дополнительных полей или alternate
  values.
- `Depends On` содержит ровно investigation v20, integration decision,
  scheduler v1 implementation и authorization v19; `Blocks` содержит только
  exact implementation v20.
- Future implementation использует только
  `{"authorization_card":"openspec/board/4.done/authorize-bounded-affected-release-profile-v20.md","authorization_id":"authorize-bounded-affected-release-profile-v20"}`,
  начинается от authorization-publishing HEAD, зависит ровно от тех же четырёх
  predecessors плюс эту authorization, блокирует только certification и
  добавляет максимум `499` production LOC.
- Published authorization v18 proof inventory остаётся единственным immutable
  command/typed-operand anchor: exact rows `35/30/48`, semantic digest
  `7147ee3c4b067486162f3dc1fee218c87eb40cbdb0d7730a9a78442da7986513`,
  full digest
  `6587ad0b9887e79f731cdf1ef25f7ff139140747ac9f4def3aeda762c1c4ae72`,
  total 35→30 ownership и exact `36 - 4 - (3 - 1) = 30` migration. V20 не
  копирует, переиздаёт или ослабляет этот proof-only artifact.
- Future admission имеет process-free Phase A, которая aggregate-валидирует
  repository, immutable registry/typed operands, origins/packages, runtime
  root и все selected task roots до любого process/Git/scheduler/write event.
  Только единый barrier разрешает bounded Phase-B probes; pre-import external
  observer и stage catalog доказывают zero process/later events на всех
  Phase-A faults и bounded non-authoritative `semantic_started:0` failure.
- Future hosted proof отдельно для `node`, `npm`, `npx` независимо парсит exact
  pinned four-step CI и major `20`, требует единственный `20.x.y`/architecture,
  проверяет каждый cache/version/arch/bin directory component без symlink
  traversal и выводит expected target/argv только из CI row и observed
  filesystem. Bounded npm/npx launcher chain остаётся contained в real version
  subtree, usable fake-first PATH не вызывается.
- Hosted negative matrix отвергает missing/relative/outside root, symlinked
  ancestor, wrong/duplicate version, architecture/token, broken/absolute/
  traversing/escaping/cyclic launcher, zero/multiple target, fake-first
  selection и hosted-to-system fallback в Phase A до первого process event.
- Future activation proof материализует полный observed row multiset и одной
  finite context-sensitive worklist от exact public affected entrypoint,
  production arguments и `supervisor=None` назначает каждой строке
  predecessor/transfer, reachability/reason и owner closure. Static projection
  получается только из exact reachable rows, без отдельного literal edge
  catalog или hardcoded owner exclusions.
- Full observed rows bidirectionally сравниваются с separately authored
  immutable `ACTIVATION_CATALOG` по source/digest, item/owner/AST path,
  context/predicates, callee/receiver, predecessor/transfer,
  reachability/reason и sink class. Pre-import dynamic child отображает calls/
  sinks на те же canonical row IDs и равен row-derived projection через
  affected→scheduler-default→broker; non-None supervisor остаётся cataloged,
  predicate-backed unreachable.
- До первой production, CI или main-spec mutation future implementation
  содержит только implementation card, same-slug OpenSpec и focused-test
  artifacts, затем сохраняет direct fingerprint-first
  `bin/changerail-evidence capture` failure с genuine non-zero exit, reachable
  saved tree и concrete missing module/symbol. Поздняя reproduction,
  zero-exit wrapper или terminal v19 evidence не доказывают chronology.
- Authorization сохраняет Unicode 16.0.0 `23/235`, strict four-stream selector
  с rename/copy old/new operands, exact full fallback, typed scheduler,
  connected guard mutants, full-only publication authority, affected/protocol
  non-authority, closed ownership и source-safe four-step CI; production/test/
  runtime LOC `0`, implementation/certification absent, prohibited history/
  full/affected/live/certification checks не запускаются.

## Change Set
- `authorize-bounded-affected-release-profile-v20`

## Verify
- Required: exact published base/remotes/object/dependencies/sole block and
  six-field object; exact future reference/relations/ceiling; immutable v18
  inventory counts/digests/migration; Phase-A/Phase-B ordering; component-safe
  CI-derived hosted oracle; unified row/worklist/projection/dynamic equality;
  original RED; successor absence; strict OpenSpec; JSON/TOML; source
  classification; current-only public scan; exact sync; whitespace and
  manifest scope.
- RED: not applicable; docs-only authorization adds no executable behavior.
- Prohibited: terminal v19 payload/evidence access, reachable history, real
  full/affected execution or benchmark, live matrix, implementation creation
  and certification checks.
- Observed: exact base/remotes/object/relations and successor absence passed;
  immutable v18 inventory `35/30/48`, both exact digests, total ownership and
  static `36-4-(3-1)=30` migration remain the sole published anchor; Phase-A/
  Phase-B, component-safe hosted and unified activation contracts are frozen
  without inventory copying; strict OpenSpec after archive `23/23`, JSON/TOML,
  source classification, byte-exact sync, zero executable LOC, whitespace,
  current-only public scan `1698/0` and manifest scope passed. Prohibited checks
  were not run.

## Archive
- `openspec/changes/archive/2026-08-30-authorize-bounded-affected-release-profile-v20/`

## Related
- `openspec/changes/authorize-bounded-affected-release-profile-v20/`
- `openspec/board/4.done/investigate-affected-release-profile-admission-hosted-activation-closure-v20.md`
- `openspec/board/4.done/authorize-bounded-affected-release-profile-v19.md`
- `openspec/changes/archive/2026-08-29-authorize-bounded-affected-release-profile-v18/proof-inventory.md`
- `openspec/specs/changerail-release-ci/spec.md`

## Result
DO complete: exact docs-only v20 authorization synchronized byte-exactly and
archived with zero production/test/runtime LOC; ready for fresh ordinary/high
review.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `authorize-bounded-affected-release-profile-v20`

### Why
Published investigation v20 требует отдельно reviewed и remotely published
authorization до любой clean executable implementation v20.

### Goal
Publish one exact docs-only authorization for the sole clean v20 implementation
while retaining the v18 immutable proof anchor and v20 proof architecture.

### Scope
- this card;
- same-slug docs-only OpenSpec artifacts;
- synchronized release-CI authorization contract and archive metadata.

### Acceptance
- Exact source object, relations, sole block, future reference, immutable
  inventory, admission/hosted/activation proof, original RED, zero LOC and
  successor dormancy are machine-checkable.

### Depends On
- `investigate-affected-release-profile-admission-hosted-activation-closure-v20`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`
- `authorize-bounded-affected-release-profile-v19`

### Related
- `openspec/changes/authorize-bounded-affected-release-profile-v20/`

## Log
- 2026-08-30 card created in a clean worktree and remote branch from exact
  published investigation v20 SHA; terminal v19 remains forensic-only and
  implementation v20 is absent.
- 2026-08-30 FF prepared one docs-only authorization change with exact object,
  relations, immutable v18 inventory reference, pre-process admission barrier,
  component-safe hosted oracle, unified activation projection and original-RED
  boundary.
- 2026-08-30 DO synchronized seven v20 authorization requirements byte-exactly,
  archived the change and passed strict OpenSpec `23/23`, JSON/TOML, source
  classification, unchanged inventory, zero executable LOC, exact relations/
  successor absence, current-only public scan `1698/0`, whitespace and
  manifest-scope gates; prohibited checks were not run.
- 2026-08-30T08:54:17Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.
