# Авторизовать bounded affected release profile v22

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
archived

## Series
- release-baseline-acceleration

## Series Index
- 59

## Source
- Published execution/activation/ancestry investigation v22
  `investigate-affected-release-profile-execution-activation-ancestry-closure-v22`,
  exact commit `6fd94a4bf964dc9551fae607f9504fd22e3b3e26`.
- Published authorization v21, immutable authorization v18 proof inventory,
  integration decision, semantic scheduler v1 and accumulated
  `changerail-release-ci` contract.
- Terminal unpublished implementation v21 card, OpenSpec, source, tests,
  CI/main-spec mutation, manifest, verdicts, logs and raw evidence remain
  forensic-only and are not read, copied, cherry-picked or accepted. Only the
  bounded chronology already published by investigation v22 is inherited.

## Summary
Опубликовать одну clean docs-only authorization v22, которая связывает sole
implementation successor с additive admitted-execution transport, atomic
descriptor-bound exec, complete row-level activation topology, anchored
runtime ancestry и новым clean original-RED episode.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`
- Independent review: one fresh `gpt-5.6-sol`/`high`
- Same-card repair budget limit/used/remaining: `1/0/1`, exhausted `false`

## Depends On
- `investigate-affected-release-profile-execution-activation-ancestry-closure-v22`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`
- `authorize-bounded-affected-release-profile-v21`

## Blocks
- `implement-bounded-affected-release-profile-v22`

## Authorization
- Investigation authorization:
  `{"investigation_card":"openspec/board/4.done/investigate-affected-release-profile-execution-activation-ancestry-closure-v22.md","investigation_id":"investigate-affected-release-profile-execution-activation-ancestry-closure-v22","successor_card":"openspec/board/3.inprogress/implement-bounded-affected-release-profile-v22.md","successor_id":"implement-bounded-affected-release-profile-v22","production_loc_ceiling":500,"allow_new_authority_or_wire_protocol":true}`

## Acceptance
- Authorization публикуется docs-only от exact investigation v22 HEAD
  `6fd94a4bf964dc9551fae607f9504fd22e3b3e26`; local/upstream/remote
  authorization branch и remote investigation branch до mutation указывают на
  этот SHA, а six-field object не имеет дополнительных полей или alternate
  values.
- `Depends On` содержит ровно investigation v22, integration decision,
  scheduler v1 implementation и authorization v21; `Blocks` содержит только
  exact implementation v22. Future implementation использует только
  `{"authorization_card":"openspec/board/4.done/authorize-bounded-affected-release-profile-v22.md","authorization_id":"authorize-bounded-affected-release-profile-v22"}`,
  начинается от authorization-publishing HEAD, зависит ровно от тех же четырёх
  predecessors плюс эту authorization, блокирует только certification и
  добавляет максимум `499` production LOC.
- Published authorization v18 proof inventory остаётся единственным immutable
  command/typed-operand anchor: exact rows `35/30/48`, semantic digest
  `7147ee3c4b067486162f3dc1fee218c87eb40cbdb0d7730a9a78442da7986513`,
  full digest
  `6587ad0b9887e79f731cdf1ef25f7ff139140747ac9f4def3aeda762c1c4ae72`,
  total 35→30 ownership и exact `36 - 4 - (3 - 1) = 30` migration. V22 не
  копирует, переиздаёт или ослабляет этот proof-only artifact.
- Published scheduler v1 `run_plan` и broker v5 `supervise` остаются
  backward-compatible. Единственное additive wire surface —
  `changerail.release-admitted-execution.v1`: one-to-one owner row содержит
  direct member либо outer executor плюс exact ordered inner admissions; все
  records связывают logical argv, deterministic physical argv/FD map,
  executable/operand ancestry, closed environment и canonical digests.
- Scheduler worker no-follow открывает exact executable/operand FDs и
  запечатывает complete nested table в bounded `memfd`; admitted broker
  запускает тот же native executable FD через FD-capable `os.execve(fd, argv,
  env)` и close-on-exec status pipe. Scripts/launchers используют pinned native
  interpreter и `/proc/self/fd/<n>` operands без kernel shebang, live PATH,
  ambient reconstruction, reopen или legacy Popen.
- Independent execution oracle отдельно выводит direct/outer/inner logical и
  physical rows из immutable inventory, pinned CI и fixture filesystem,
  наблюдает exec inputs, FD identities, sealed bundle и post-exec `/proc`
  identity bidirectionally. Fake-first PATH, changed environment/member/bundle,
  pre-open swap, legacy raw-token execution и malformed neighbor fail before
  start; post-open rename не меняет executed object.
- Static observer материализует полный multiset imports/bindings/functions/
  predicates/calls/raw sinks и запускает finite context-sensitive worklist
  только от exact public affected entrypoint. Каждая row имеет exact source,
  digest, owner, AST path/span, context/predicates, callee/receiver set,
  predecessor/transfer, reachability/reason и sink class; separately authored
  immutable `ACTIVATION_CATALOG` сравнивается по каждому полю без sentinel,
  self-derived, unknown или counts-only shortcuts.
- Pre-import clean child регистрирует test-only `sitecustomize`, nonce-bound
  collector, import guard, opcode/profile/audit hooks и at-fork topology до
  production import/spawn/exec. Каждый raw occurrence однозначно map-ится на
  catalog row; только identical timing-loop activation keys collapse, а
  normalized dynamic topology, static projection и catalog projection равны
  bidirectionally через public→scheduler→broker/group/inner path.
- Runtime root допускается только как empty lexical direct child exact real
  anchor. Directory-FD walk с `O_NOFOLLOW` проверяет каждую ancestry identity,
  lexical/real spelling, mount/alias/repetition/outside transitions и повторяет
  chain/emptiness непосредственно до scheduler reservation; любой neighbor или
  drift даёт `semantic_started:0` и zero later process/Git/scheduler/write/
  mutation events.
- Future implementation создаёт новый genuine direct fingerprint-first RED до
  production, CI или main-spec mutation и использует только disposable real-
  Git fixtures, exact harmless command allowlist и retained process/event
  evidence. Workspace diagnostics не вызывают public affected или exact-full
  fallback, а terminal v21 episode/evidence не переиспользуется.
- Authorization сохраняет Unicode 16.0.0 `23/235`, strict committed/staged/
  unstaged/untracked selection с rename/copy operands, deterministic exact-full
  fallback, typed scheduler, exact hosted `node/npm/npx` and source-safe
  four-step CI guards, full-only publication authority и affected/protocol
  `authoritative:false`.
- Authorization меняет только эту card, same-slug OpenSpec, synchronized main
  release-CI spec и archive metadata; production/test/runtime LOC `0`,
  implementation/certification отсутствуют, reachable history, real full/
  affected execution or benchmark, live matrix и certification checks не
  запускаются. Publication требует один fresh ordinary `gpt-5.6-sol/high`
  review.

## Change Set
- `authorize-bounded-affected-release-profile-v22`

## Verify
- Required: exact published base/remotes/object/dependencies/sole block;
  future reference/relations/ceiling; immutable v18 inventory; additive sealed
  admitted transport and atomic FD exec; independent execution oracle; complete
  worklist/catalog/topology; anchored ancestry; clean original RED; accumulated
  guards; successor absence; strict OpenSpec; JSON/TOML; source classification;
  current-only public scan; exact sync; whitespace and manifest scope.
- RED: not applicable; docs-only authorization adds no executable behavior.
- Prohibited: terminal v21 payload/evidence access, reachable history, real
  full/affected execution or benchmark, live matrix, implementation creation
  and certification checks.
- Observed: exact published base and clean local/upstream/remote authorization
  branch passed; exact object/relations and successor absence passed; immutable
  v18 inventory `35/30/48`, both exact digests, total ownership and static
  migration remain the sole published anchor; eight requirements synchronized
  byte-exactly; strict OpenSpec after archive `23/23`, JSON/TOML, source
  classification, zero executable LOC, whitespace, current-only public scan
  `1722/0` and manifest scope passed. Prohibited checks were not run.

## Archive
- `openspec/changes/archive/2026-08-30-authorize-bounded-affected-release-profile-v22/`

## Related
- `openspec/changes/authorize-bounded-affected-release-profile-v22/`
- `openspec/board/4.done/investigate-affected-release-profile-execution-activation-ancestry-closure-v22.md`
- `openspec/board/4.done/authorize-bounded-affected-release-profile-v21.md`
- `openspec/changes/archive/2026-08-29-authorize-bounded-affected-release-profile-v18/proof-inventory.md`
- `openspec/specs/changerail-release-ci/spec.md`

## Result
DO complete: exact docs-only v22 authorization synchronized byte-exactly and
archived with zero production/test/runtime LOC; ready for deterministic
preflight and fresh ordinary/high review.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `authorize-bounded-affected-release-profile-v22`

### Why
Published investigation v22 требует separately reviewed и remotely published
authorization до любой clean executable implementation v22.

### Goal
Publish one exact docs-only authorization for the sole clean v22 implementation
while retaining immutable execution, activation, ancestry and authority floors.

### Scope
- this card;
- same-slug docs-only OpenSpec artifacts;
- synchronized release-CI authorization contract and archive metadata.

### Acceptance
- Exact source object, relations, sole block, future reference, immutable
  inventory, admitted FD execution, activation topology, anchored ancestry,
  clean RED chronology, zero LOC and successor dormancy are machine-checkable.

### Depends On
- `investigate-affected-release-profile-execution-activation-ancestry-closure-v22`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`
- `authorize-bounded-affected-release-profile-v21`

### Related
- `openspec/changes/authorize-bounded-affected-release-profile-v22/`

## Log
- 2026-08-30 card created in a clean worktree and remote branch from exact
  published investigation v22 SHA; terminal v21 remains forensic-only and
  implementation v22 is absent.
- 2026-08-30 FF prepared one docs-only authorization change with exact object,
  relations, immutable v18 inventory, admitted FD execution, complete
  activation topology, anchored ancestry and clean original-RED boundary.
- 2026-08-30 DO synchronized eight v22 authorization requirements byte-exactly,
  archived the change and passed strict OpenSpec `23/23`, JSON/TOML, source
  classification, unchanged inventory, zero executable LOC, exact relations/
  successor absence, current-only public scan `1722/0`, whitespace and
  manifest-scope gates; prohibited checks were not run.
- 2026-08-30T19:30:32Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.
