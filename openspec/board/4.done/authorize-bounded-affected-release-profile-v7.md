# Авторизовать bounded affected release profile v7

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
archived

## Series
- release-baseline-acceleration

## Series Index
- 19

## Source
- Published installed-origin investigation decision
  `rescue-affected-release-profile-installed-origin-boundary-v7`, exact commit
  `932b3c5643b009e5a2f372c4c6b4ca803cac1d87`.
- Published integration decision, semantic scheduler v1 and affected v6
  authorization.
- Terminal unpublished affected v6 implementation and all of its card, code,
  manifest, verdicts, logs and evidence remain forensic-only.

## Summary
Авторизовать ровно один clean implementation successor для affected profile v7
с exact effective-interpreter `purelib`/`platlib` origin boundary, connected
production-default proof и `<=499` production LOC.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`
- Independent review: one fresh `gpt-5.6-sol`/`high`
- Same-card repair budget limit/used/remaining: `1/0/1`, exhausted `false`

## Depends On
- `rescue-affected-release-profile-installed-origin-boundary-v7`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`
- `authorize-bounded-affected-release-profile-v6`

## Blocks
- `implement-bounded-affected-release-profile-v7`

## Authorization
- Investigation authorization:
  `{"investigation_card":"openspec/board/4.done/rescue-affected-release-profile-installed-origin-boundary-v7.md","investigation_id":"rescue-affected-release-profile-installed-origin-boundary-v7","successor_card":"openspec/board/3.inprogress/implement-bounded-affected-release-profile-v7.md","successor_id":"implement-bounded-affected-release-profile-v7","production_loc_ceiling":500,"allow_new_authority_or_wire_protocol":true}`

## Acceptance
- Authorization публикуется docs-only от exact investigation HEAD
  `932b3c5...`; remote authorization branch до mutation указывает ровно на этот
  SHA, а six-field object выше не имеет дополнительных полей или wrappers.
- `Depends On` содержит ровно v7 investigation decision, integration decision,
  scheduler v1 implementation и affected v6 authorization; `Blocks` содержит
  только exact v7 implementation.
- Future implementation использует только
  `{"authorization_card":"openspec/board/4.done/authorize-bounded-affected-release-profile-v7.md","authorization_id":"authorize-bounded-affected-release-profile-v7"}`,
  начинается от authorization-publishing HEAD, добавляет не более 499
  production LOC, зависит ровно от четырех predecessors выше плюс эту
  authorization и блокирует только `certify-accelerated-release-loop-v1`.
- До первой production, CI или main-spec mutation future v7 содержит только
  implementation card, same-slug OpenSpec и focused-test artifacts и напрямую
  запускает реальный failing focused test через `bin/changerail-evidence capture`.
- Captured RED command сначала печатает
  `bin/changerail-review-verdict fingerprint --workspace .`, затем сохраняет
  настоящий non-zero focused-test exit; retained entry содержит `failed`,
  non-zero exit, `tree_sha`, `diff_fingerprint` и missing production
  module/symbol, а saved tree существует до production mutation.
- Fresh reviewer восстанавливает saved RED tree относительно authorization
  HEAD и fail closed доказывает отсутствие production, CI или main-spec
  mutations; поздняя reproduction или zero-exit wrapper не принимаются.
- Effective Python identity и allowed distribution origins выводятся только из
  resolved exact `sysconfig.get_paths()` keys `purelib` и `platlib`; missing,
  wrong-type, symlink, resolution или ambiguity faults fail before semantics.
- Каждая exact runtime/dev distribution имеет exact pin и `locate_file("")`
  origin, равный admitted package root; `stdlib`, `platstdlib`, `scripts`,
  `data`, `include`, child/prefix и arbitrary existing paths отклоняются.
- Ruff `0.6.9` связывает exact framed version, admitted installed origin и
  executable в selected-interpreter bin; OpenSpec сохраняет exact offline
  `1.3.1`; aggregate admission завершается до Git selection, scheduler и любой
  filesystem mutation с bounded `semantic_started: 0` fault report.
- Focused proof использует production-default `purelib`/`platlib` happy neighbor
  и connected production-guard mutants для всех non-package roots, wrong
  pin/version/Python/Ruff origin, `MAX_PATH`, deduplicated `MAX_PATHS`, каждого
  four-stream и aggregate `MAX_GIT_BYTES`, resolved-base и runtime-output
  ordering/type guards; injected allowlist не может удовлетворить proof.
- Future v7 сохраняет exact 35-ID digest и 35→30 ownership, strict bounded
  four-stream selector, typed scheduler rows и integer jobs `1|4`, full-only
  authority, exact source-safe four-step CI и receipt/capture/marker/cache
  non-authority из published sources.
- Terminal unpublished v6 payload/runtime state не читаются, не копируются,
  не cherry-pick-ятся и не принимаются; authorization добавляет
  production/test/runtime LOC `0`, не создаёт successor/code/test/dependency/
  schema/CI/runtime authority и не запускает history, real full/affected,
  benchmark, live matrix или certification.

## Change Set
- `authorize-bounded-affected-release-profile-v7`

## Verify
- GREEN required: exact source/remote/object/reference/dependencies/sole block,
  successor absence, retained RED/reconstruction boundary, exact installed
  origins, production-default connected proof, preserved repaired-v6 floor,
  strict OpenSpec, JSON/TOML, source classification, current-only public scan,
  archive/main sync, whitespace, manifest scope and ordinary/high preflight.
- GREEN retained: `safe-base-remote-authorization-v7`,
  `docs-green-authorization-v7-prearchive` and
  `post-archive-contract-scope-v7-reviewed` bind exact base/remote, authorization
  shape, successor absence, archived delta/main sync, allowed static/current
  checks and docs-only production/test/runtime LOC `0`.
- RED: not applicable; docs-only authorization adds no executable behavior.
- Prohibited: history, full baseline, affected execution/benchmark, live matrix,
  successor creation/implementation, certification and terminal v6 reuse.

## Archive
- `openspec/changes/archive/2026-08-27-authorize-bounded-affected-release-profile-v7/`

## Related
- `openspec/changes/authorize-bounded-affected-release-profile-v7/`
- `openspec/board/4.done/rescue-affected-release-profile-installed-origin-boundary-v7.md`
- `openspec/specs/changerail-release-ci/spec.md`

## Result
DO complete: one exact docs-only v7 authorization is synchronized and archived.
Implementation successor, focused tests and executable payload remain absent.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `authorize-bounded-affected-release-profile-v7`

### Why
Published v7 investigation requires a separately reviewed and published
authorization before any v7 implementation card, test or executable work may
exist.

### Goal
Publish one exact bounded authorization for the sole clean v7 implementation.

### Scope
- this card;
- same-slug OpenSpec artifacts;
- synchronized `changerail-release-ci` specification and archive metadata.

### Acceptance
- Exact source object, future reference, dependencies, sole block, retained RED
  boundary, installed-origin admission, connected default-path and preserved
  repaired-v6 proof floor, LOC and dormancy contracts are machine-checkable.

### Depends On
- `rescue-affected-release-profile-installed-origin-boundary-v7`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`
- `authorize-bounded-affected-release-profile-v6`

### Related
- `openspec/changes/authorize-bounded-affected-release-profile-v7/`

## Log
- 2026-08-27 created in a clean worktree and remote branch from exact published
  v7 investigation HEAD; successor and terminal v6 forensic payload/evidence
  were not created, read or imported.
- 2026-08-27 FF produced one exact apply-ready docs-only authorization change;
  successor, executable LOC and prohibited evidence remain absent.
- 2026-08-27 DO retained exact source/remote and lineage/absence evidence,
  synchronized five release-CI requirements, archived the same-slug change and
  passed strict/config/classification/current-public/whitespace checks with
  production/test/runtime LOC `0`; prohibited execution remained absent.
- 2026-08-27T11:23:27Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.
