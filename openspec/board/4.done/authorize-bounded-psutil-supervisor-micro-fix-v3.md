# Авторизовать bounded psutil supervisor micro-fix v3

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
archived

## Series
- release-baseline-acceleration

## Series Index
- 02R10-S3A

## Source
- Published decision `decide-bounded-unpublished-terminal-micro-fix-boundary`,
  commit `faacde9f86868b61da38eb9194080bc4bfdc9339`.
- The frozen failed S2 candidate remains generic forensic source material only;
  it is unpublished and supplies no authority, evidence, verdict, history,
  log, receipt or manifest.

## Summary
Опубликовать единственный bounded authorization source для будущего S3
psutil-backed release child supervisor micro-fix, сохранив exact lineage,
clean reconstruction и fresh R1-R7 proof contract без создания successor.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`
- Implementation: docs-only deterministic
- Independent review: one fresh `gpt-5.6-sol`/`high` pending
- Same-card repair/retry/rescue budget limit/used/remaining: `0/0/0`, exhausted `true`

## Depends On
- `decide-bounded-unpublished-terminal-micro-fix-boundary` (published
  `faacde9f86868b61da38eb9194080bc4bfdc9339`)

## Blocks
- `deliver-psutil-backed-release-child-supervisor-v3`
- downstream refresh remains blocked until S3 is published.

## Authorization
- Investigation authorization: `{"investigation_card":"openspec/board/4.done/decide-bounded-unpublished-terminal-micro-fix-boundary.md","investigation_id":"decide-bounded-unpublished-terminal-micro-fix-boundary","successor_card":"openspec/board/3.inprogress/deliver-psutil-backed-release-child-supervisor-v3.md","successor_id":"deliver-psutil-backed-release-child-supervisor-v3","production_loc_ceiling":500,"allow_new_authority_or_wire_protocol":true}`

## Acceptance
- This authorization depends on the exact published decision and blocks only
  `deliver-psutil-backed-release-child-supervisor-v3`; the decision blocks this
  authorization and that successor.
- The future successor depends on both sources and uses only
  `{"authorization_card":"openspec/board/4.done/authorize-bounded-psutil-supervisor-micro-fix-v3.md","authorization_id":"authorize-bounded-psutil-supervisor-micro-fix-v3"}`.
  It remains absent from this payload.
- The only successor is eligible only when its candidate is unpublished, this
  exact published authorization is valid, all prior findings are independently
  closed and the latest cycle contains exactly one new isolated blocker. It
  starts clean from this authorization-publishing HEAD, remains in its exact
  authorized production paths, adds at most 499 production LOC relative to
  that HEAD, and expands no scope, dependency, schema or ownership.
- The future successor may mechanically reconstruct code and tests from the
  frozen failed candidate only as source material. It cannot reuse terminal
  verdict, review history, log, receipt, manifest or other evidence, and must
  rerun every connected R1-R7 proof.
- R7 treats pipe EOF only as stream state. Completion requires a terminal
  leader observation or `execution_timeout`, then cleanup under the existing
  bounded cleanup contract.
- The successor has no credential, mutation, live-admission or final authority;
  it receives one implementation attempt, one fresh Sol/high review and exact
  repair/retry/rescue budget `0/0/0`. S3 and downstream refresh remain dormant
  until S3 is published.
- This docs-only payload adds production, test and runtime LOC `0`; it creates
  no successor card/code and accepts no history, full baseline, live execution,
  review, commit or push evidence.

## Change Set
- `authorize-bounded-psutil-supervisor-micro-fix-v3`

## Verify
- GREEN: strict target, `changerail-release-ci` and all OpenSpec validation;
  exact object, field-count/order, reciprocal lineage, successor absence,
  eligibility, fresh-proof, EOF and dormancy contract checks.
- GREEN: `.mcp.json` JSON and `.codex/config.toml` TOML parsing,
  current-only public-surface and source-classification scans, tracked plus
  explicit-untracked whitespace, ignored-manifest scope and normalized
  ordinary/high preflight.
- No history scan, full release baseline, live execution, successor, review,
  commit or push is run or accepted for this docs-only delivery.

## Archive
- `openspec/changes/archive/2026-08-26-authorize-bounded-psutil-supervisor-micro-fix-v3/`

## Related
- `openspec/board/4.done/decide-bounded-unpublished-terminal-micro-fix-boundary.md`
- `openspec/specs/changerail-release-ci/spec.md`

## Result
DO completed: one docs-only authorization source synchronized the exact v3
lineage, clean reconstruction, terminal-evidence non-reuse, fresh R1-R7 proof,
R7 EOF, dormant downstream and `0/0/0` contract in `changerail-release-ci`.
The same-slug change is archived. Production/test/runtime LOC: `0`. Successor
card/code and downstream refresh remain absent and blocked. The payload awaits
one fresh independent ordinary/high review.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `authorize-bounded-psutil-supervisor-micro-fix-v3`

### Why
The published terminal micro-fix decision permits exactly one new source of
authority before an S3 successor can exist, but the decision itself is not that
publishable authorization.

### Goal
Publish the exact six-field authorization and retain the clean bounded S3
micro-fix contract without creating or implementing its successor.

### Scope
- This card, same-slug OpenSpec artifacts, synchronized `changerail-release-ci`
  specification and archive metadata only; production/test/runtime LOC `0`.

### Acceptance
- Exact six-field object, reciprocal decision/authorization/future-successor
  lineage, exact two-field future reference and `<=499` authorization-HEAD
  implementation limit are retained.
- Eligibility, clean reconstruction, evidence non-reuse, fresh R1-R7 proof,
  R7 EOF semantics, dormancy and `0/0/0` budget remain explicit.
- Successor card/code and all downstream refresh work remain absent and blocked.

### Depends On
- `decide-bounded-unpublished-terminal-micro-fix-boundary`

### Related
- `openspec/changes/authorize-bounded-psutil-supervisor-micro-fix-v3/`

## Log
- 2026-08-26 FF created one same-slug docs-only authorization from the exact
  published terminal micro-fix decision. The successor remains absent.
- 2026-08-26 DO synchronized `changerail-release-ci`, archived only this
  same-slug docs-only change and moved this card to `3.inprogress` for one
  fresh ordinary/high review. No successor, history/full/live evidence, review,
  commit or push was created or accepted.
- 2026-08-26T05:36:51Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.
