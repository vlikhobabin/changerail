# Исследовать composite-command и dynamic projection closure affected profile v21

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
archived

## Series
- release-baseline-acceleration

## Series Index
- 55

## Source
- Последняя safe published authorization boundary:
  `authorize-bounded-affected-release-profile-v20`, exact commit
  `e04dc38582490339d55876acc512623a0547a2ec`.
- Terminal unpublished implementation v20 получила fresh ordinary/high
  `NO-GO`, acceptance `5/11`, findings `6/0/0`. Единственный same-card rescue
  не расходуется: published scheduler singular-command schema несовместима с
  immutable three-command physical group, а public-entry exact-row dynamic
  equality требует нового proof design.
- Только validated counters и шесть finding-class summaries переходят в эту
  investigation. V20 implementation card/OpenSpec/source/tests/CI/main-spec
  mutation, manifest, verdicts, logs и raw evidence являются forensic-only и
  далее не читаются, не копируются, не cherry-pick-ятся и не используются как
  proof.

## Summary
Опубликовать clean docs-only decision для v21, который вводит bounded nested
broker boundary для immutable sequential group, exact scheduler-envelope
validation и callsite-qualified static/dynamic projection от public affected
entrypoint, одновременно закрывая runtime-root, hosted-launcher и sole-CI
guards.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`
- Independent review: one fresh `gpt-5.6-sol`/`high`
- Same-card repair budget limit/used/remaining: `1/1/0`, exhausted `true`

## Depends On
- `authorize-bounded-affected-release-profile-v20`
- `investigate-affected-release-profile-admission-hosted-activation-closure-v20`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`

## Blocks
- `authorize-bounded-affected-release-profile-v21`
- `implement-bounded-affected-release-profile-v21`
- `certify-accelerated-release-loop-v1`

## Authorization
- Future v21 implementation authorization:
  `{"investigation_card":"openspec/board/4.done/investigate-affected-release-profile-composite-command-dynamic-closure-v21.md","investigation_id":"investigate-affected-release-profile-composite-command-dynamic-closure-v21","successor_card":"openspec/board/3.inprogress/implement-bounded-affected-release-profile-v21.md","successor_id":"implement-bounded-affected-release-profile-v21","production_loc_ceiling":500,"allow_new_authority_or_wire_protocol":true}`

## Acceptance
- Decision создаётся docs-only от exact published authorization v20 SHA;
  local/upstream/remote investigation branch и remote authorization branch до
  mutation указывают на этот SHA, terminal v20 payload/evidence не
  импортируется, а chronology сохраняет review `5/11`, findings `6/0/0` и
  нерасходованный repair вследствие доказанной contract incompatibility.
- Composite physical owner сохраняет immutable v18 row с тремя exact argv и
  ровно один scheduler task/result. Scheduler запускает отдельно typed
  repository group-executor command под published outer broker; executor
  получает только owner ID и expected registry digest, импортирует production
  registry, bidirectionally сверяет exact group и последовательно вызывает
  published broker для каждого original argv с bounded timeouts. Любой nested
  malformed/fail/timeout/cleanup result завершает group non-zero; wrapper,
  direct `subprocess`, shell, skipped/reordered/duplicate argv и raw inventory
  transfer fail closed.
- Independent composite oracle парсит immutable v18 group и production
  registry отдельно, наблюдает outer scheduler/broker row и все три ordered
  inner broker argv, доказывает one physical result и zero later argv после
  первого failure. Scheduler/broker source и wire schema остаются exact-digest
  published v1/v5 и не меняются.
- Scheduler summary admission требует exact closed top-level fields/version,
  requested jobs, exact ordered selected owner IDs и для каждого row exact
  published field set/cross-field passing tuple. Missing, duplicate, unknown,
  reordered, empty, extra, malformed, failing or cross-owner row, wrong status,
  jobs/version/digest не может создать pass или full authority.
- Static observer материализует полный row multiset и запускает одну finite
  context-sensitive worklist только от
  `run_profile("affected", base=<known-docs-base>, jobs=1,
  environment=<production-shaped>)`; `main(..., supervisor=None)` достигается
  только через public path. Каждая row получает source/digest, owner, canonical
  AST path/span, context/predicates, callee/receiver, predecessor/transfer,
  reachability/reason и sink class; static projection является только filter
  exact reachable call/raw-sink rows.
- Pre-import clean child использует disposable real-Git repository, harmless
  exact selected commands и production `supervisor=None`. Opcode/call/audit
  observer отображает каждый qualified Python/C call и raw sink по caller code
  identity, bytecode position и AST span на canonical row ID. Dynamic multiset
  bidirectionally равен static row-derived projection через public affected →
  scheduler default → outer broker → group executor → inner brokers; zero,
  ambiguous, multiple или unmapped rows fail closed. Non-None supervisor rows
  остаются cataloged и false-predicate unreachable; отсутствие trace не
  является proof.
- Before the public child starts, the harness creates a test-only
  `sitecustomize.py`, a nonce-bound bounded Unix-datagram collector and exact
  expected source-digest/role catalog. Inherited `PYTHONPATH` installs
  registration, import guard, opcode/profile/audit hooks and at-fork child
  registration before production import in the public interpreter, every
  scheduler `spawn` interpreter and the execed group executor. Closed datagrams
  carry nonce, pid/ppid, role, per-process sequence and canonical callsite data;
  missing/late/duplicate/bad-nonce registration, unknown production-importing
  role, disconnected parentage, overflow, timeout or event before registration
  fails proof. Harness registration/transport remains test-only and cannot be
  accepted as production reachability or authority.
- Existing runtime root допускается только как real empty directory; любой
  child entry, включая unknown, dangling, symlinked или aliased neighbor,
  отвергается в Phase A с zero process/later events. Hosted npm/npx launcher
  допускает только exact relative
  `../lib/node_modules/npm/bin/{npm|npx}-cli.js`; obfuscated traversal,
  alternate spelling и все v20 negative neighbors отвергаются до process.
- CI oracle независимо парсит exact YAML object и каждый scalar/block command.
  Dependency step равен exact three-command block, final step равен единственному
  exact full-release scalar, а любое второе occurrence, wrapper, chain,
  indirect/inactive copy или execution-bearing extra field fail closed.
- Future authorization зависит ровно от этой investigation, integration
  decision, scheduler v1 и authorization v20 и блокирует только implementation
  v21. Future implementation зависит ровно от тех же predecessors плюс
  authorization v21, блокирует только certification, начинается от
  authorization-publishing HEAD, создаёт новый genuine original RED и
  добавляет максимум `499` production LOC.
- Investigation сохраняет published v18 proof inventory как единственный
  immutable anchor, exact `35/30/48`, semantic digest
  `7147ee3c4b067486162f3dc1fee218c87eb40cbdb0d7730a9a78442da7986513`,
  full digest
  `6587ad0b9887e79f731cdf1ef25f7ff139140747ac9f4def3aeda762c1c4ae72`,
  total 35→30 ownership, `36 - 4 - (3 - 1) = 30`, Unicode `23/235`, strict
  four-stream selection, full-only authority, affected/protocol non-authority
  и source-safe four-step CI.
- Investigation меняет только эту card, same-slug OpenSpec, synchronized main
  release-CI spec и archive metadata; production/test/runtime LOC `0`, future
  authorization/implementation/certification отсутствуют, а reachable history,
  real full/affected execution or benchmark, live matrix и certification
  checks не запускаются.

## Change Set
- `investigate-affected-release-profile-composite-command-dynamic-closure-v21`

## Verify
- Required: exact safe base/remotes and forensic boundary; published singular
  scheduler versus immutable group compatibility proof; nested broker design;
  closed scheduler summary; public-entry callsite-qualified projection;
  runtime/hosted/CI guards; exact future lineage/ceiling; successor absence;
  strict OpenSpec; JSON/TOML; source classification; current-only public scan;
  exact sync; whitespace and manifest scope.
- RED: not applicable; docs-only investigation.
- Prohibited: terminal v20 payload/evidence access after handoff, history, real
  full/affected execution or benchmark, live matrix and certification checks.
- Observed: exact local/upstream/remote authorization base passed; published
  scheduler task schema has singular `command` while immutable
  `drift.generated-fixture` has three commands; byte-exact main-spec sync,
  strict OpenSpec after archive `23/23`, JSON/TOML, source classification,
  zero executable LOC, successor absence and whitespace passed; current-only
  public scan passed `1704/0`. Prohibited checks were not run.

## Archive
- `openspec/changes/archive/2026-08-30-investigate-affected-release-profile-composite-command-dynamic-closure-v21/`

## Related
- `openspec/changes/investigate-affected-release-profile-composite-command-dynamic-closure-v21/`
- `openspec/board/4.done/authorize-bounded-affected-release-profile-v20.md`
- `openspec/board/4.done/investigate-affected-release-profile-admission-hosted-activation-closure-v20.md`
- `openspec/changes/archive/2026-08-29-authorize-bounded-affected-release-profile-v18/proof-inventory.md`
- `openspec/specs/changerail-release-ci/spec.md`

## Result
DO complete: docs-only v21 composite-command/dynamic-projection decision
synchronized byte-exactly and archived with zero production/test/runtime LOC;
ready for deterministic preflight and fresh ordinary/high review.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `investigate-affected-release-profile-composite-command-dynamic-closure-v21`

### Why
Terminal v20 выявила несовместимость singular scheduler command с immutable
sequential group и disconnected public-entry dynamic projection, которые нельзя
исправить bounded same-card micro-fix без нового design authority.

### Goal
Опубликовать feasible independent proof и execution architecture для всех шести
v20 finding classes до любой v21 authorization или executable successor.

### Scope
- this card;
- same-slug docs-only OpenSpec artifacts;
- synchronized release-CI decision contract and archive metadata.

### Acceptance
- Каждый acceptance criterion карточки проверяем из published sources,
  independently authored future proof contracts и exact lineage без terminal
  payload или prohibited execution.

### Depends On
- `authorize-bounded-affected-release-profile-v20`
- `investigate-affected-release-profile-admission-hosted-activation-closure-v20`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`

### Related
- `openspec/changes/investigate-affected-release-profile-composite-command-dynamic-closure-v21/`

## Log
- 2026-08-30 card created in a clean worktree and remote branch from exact
  published authorization v20 SHA; only validated terminal counters, six
  finding classes and published-source compatibility proof crossed the
  forensic boundary.
- 2026-08-30 FF prepared one docs-only investigation change with nested broker
  composite identity, closed scheduler envelope, public-entry exact-callsite
  projection and runtime/hosted/CI guard closure.
- 2026-08-30 DO synchronized six v21 requirements byte-exactly, archived the
  change and passed strict OpenSpec `23/23`, JSON/TOML, source classification,
  zero executable LOC, successor absence, current-only public scan `1704/0`
  and whitespace; prohibited checks were not run.
- 2026-08-30 fresh review cycle 1 returned `NO-GO`, acceptance `10/11`, one
  docs blocker: interpreter-local hooks lacked a cross-process bootstrap/event
  boundary for scheduler spawn and group-executor exec. The sole same-card
  rescue froze a test-only pre-import sitecustomize bootstrap, nonce-bound
  bounded datagram collector, at-fork registration and connected registration/
  topology fault matrix; production scope remains unchanged.
- 2026-08-30T11:08:35Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.
