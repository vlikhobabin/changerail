# Исследовать origin identity и discriminating proof affected profile v15

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
archived

## Series
- release-baseline-acceleration

## Series Index
- 38

## Source
- Latest safe published affected-profile reference: authorization v14 commit
  `0d0d571c74efe1ad9d1473effed0f12adcc8252d`.
- Published investigation v14, integration decision, semantic scheduler v1 and
  accumulated `changerail-release-ci` contract.
- Terminal unpublished `implement-bounded-affected-release-profile-v14`
  completed cycle 1 with five blockers, consumed its sole repair and completed
  fresh cycle 2 with `5/8` acceptance, two blockers, one minor finding and
  rescue budget `1/1/0` exhausted. Only validated verdict summaries and
  counters cross this boundary; its card, OpenSpec payload, code, tests, CI,
  main-spec mutation, manifest, logs and raw evidence are forensic-only and are
  not implementation inputs.

## Summary
Опубликовать clean docs-only investigation/design decision перед executable
v15: заменить live-`PATH` self-freezing независимой source-authored origin
policy и сделать scheduler/selector proof различающим alias, ignored status и
dropped rename/copy operand regressions.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`
- Lineage escalation: pre-import origin identity и source-connected proof
  повторились после обязательной v14 investigation и единственной repair;
  поэтому эта карточка является новым investigation/simplification boundary,
  а не продолжением terminal executable payload.
- Independent review: one fresh `gpt-5.6-sol`/`high`
- Same-card repair budget limit/used/remaining: `1/1/0`, exhausted `true`

## Depends On
- `authorize-bounded-affected-release-profile-v14`
- `investigate-affected-release-profile-exhaustive-guard-closure-v14`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`

## Blocks
- `authorize-bounded-affected-release-profile-v15`
- `implement-bounded-affected-release-profile-v15`
- `certify-accelerated-release-loop-v1`

## Authorization
- Future v15 implementation authorization:
  `{"investigation_card":"openspec/board/4.done/investigate-affected-release-profile-origin-proof-discrimination-v15.md","investigation_id":"investigate-affected-release-profile-origin-proof-discrimination-v15","successor_card":"openspec/board/3.inprogress/implement-bounded-affected-release-profile-v15.md","successor_id":"implement-bounded-affected-release-profile-v15","production_loc_ceiling":500,"allow_new_authority_or_wire_protocol":true}`

## Acceptance
- Decision создаётся docs-only из exact safe authorization v14 commit
  `0d0d571c...`; local HEAD и remote investigation branch до mutation указывают
  ровно на этот SHA. Concise chronology сохраняет cycle 1, sole repair, cycle 2
  и exhausted budget, но terminal v14 payload/evidence не импортируются.
- V14 больше не исправляется и не публикуется. До отдельной remotely published
  v15 authorization запрещены implementation v15 card, focused test,
  production, CI и executable main-spec mutation.
- Future origin inventory независимо и immutable задаёт каждому executable
  token ровно один typed descriptor и source-authored canonical resolver.
  Repository tools разрешаются только относительно exact repository root;
  Python равен real `sys.executable`; Ruff разрешается только как exact sibling
  effective interpreter scripts root; system/Node tools разрешаются только из
  закрытых generic per-token candidate roots/relative targets. Live `PATH`
  никогда не создаёт expected origin: он только сравнивается с canonical
  result, а zero/multiple candidate, symlink/type/access mismatch или alternate
  usable target fail closed до probe, Git, scheduler и filesystem mutation.
- Pre-import proof запускает отдельный clean child с fake executable уже первым
  в `PATH` до import. Fake возвращает exact expected version и exit `0`, но
  public runner обязан выдать bounded `authoritative:false`,
  `semantic_started:0`; marker доказывает, что fake probe, Git, scheduler и
  runtime creation не выполнялись. Отдельные cases покрывают Python/Ruff,
  Git, Node/npm/npx и repo OpenSpec target.
- Future proof владеет независимым immutable requirement catalog и отдельным
  executable mutant catalog. Их ids сравниваются bidirectionally, но count или
  marker-only suffix не являются evidence. Каждый заявленный distinct mutant
  изменяет один semantic AST/source node/operator/operand, имеет отличающийся
  canonical AST digest без test-only marker и наблюдается через public
  `profile.main`, runner или `run_smoke`. Каждый data case начинается от своего
  exact passing neighbor и выполняется независимо; несколько cases могут
  ссылаться на один semantic guard mutant, но не считать его разными mutants.
  Reused edit под разными labels, early return, no-op и earlier-fault masking
  запрещены.
- Scheduler ownership oracle строит closed import/call graph и допускает ровно
  один unaliased `run_plan` import и один lexical depth-one direct call. Alias
  import/call, module-qualified call, assignment alias, wrapper, `getattr`,
  dynamic dispatch, duplicate/extra call и alternate runner entrypoint обязаны
  быть executable source mutants и отклоняться публичным proof.
- Selector proof для committed, staged и unstaged streams проверяет exact
  ordered selected-ID tuple для каждого A/M/D и R/C `000`, interior, `100`, а
  не только membership safety floor. Owner-distinct old/new paths доказывают
  оба operands. Actual-source mutants, игнорирующие любой A/M/D, любой stream
  или old/new operand, обязаны изменить public selection и провалить oracle;
  untracked, base, framing, UTF-8, path/count/per-stream/aggregate bounds,
  self/unknown и timeout/nonzero/stderr остаются closed exact fallback matrix.
- Future v15 сохраняет опубликованный v14 accumulated floor: fresh original
  pre-production RED, Unicode 16.0.0 `23/235`, exact 35-ID digest и 35-to-30
  ownership, aggregate descriptor/package/runtime/task-root admission, typed
  scheduler rows, bounded scheduler/summary failures, full-only authority,
  source-safe four-step CI, resolved-base guards и protocol-artifact
  non-authority. Никакие v14 executable artifacts не переиспользуются.
- Verification claims используют только reconstructable current evidence или
  retained historical evidence с exact entry. Production LOC считается ровно
  project source-classification/preflight contract; альтернативный manual count
  не публикуется как normative claim.
- Единственный future order: эта investigation, docs-only
  `authorize-bounded-affected-release-profile-v15`, clean
  `implement-bounded-affected-release-profile-v15`, затем certification.
  Future authorization зависит ровно от этой decision, integration decision,
  scheduler v1 и authorization v14 и блокирует только implementation v15.
  Future implementation использует только v15 two-field authorization,
  зависит ровно от этой investigation, integration decision, scheduler v1,
  authorization v14 и authorization v15, блокирует только certification,
  начинается от authorization-publishing HEAD и добавляет не более `499`
  production LOC.
- Investigation добавляет production/test/runtime LOC `0` и меняет только
  свою card, same-slug OpenSpec artifacts, synchronized release-CI spec и
  archive metadata. Authorization/implementation v15 и certification остаются
  отсутствующими.
- Reachable history, real full baseline, affected execution/benchmark, live
  matrix и certification checks не запускаются и не принимаются; публикацию
  gate-ит один fresh ordinary `gpt-5.6-sol/high` review.

## Change Set
- `investigate-affected-release-profile-origin-proof-discrimination-v15`

## Verify
- Required: exact safe base/remote, terminal-v14 concise chronology and
  forensic boundary, repeated-defect escalation, independent origin resolver,
  unique discriminating scheduler/selector proof, exact future
  object/order/dependencies/ceiling, successor absence, strict OpenSpec,
  JSON/TOML, source classification, current-only public scan, exact archive/main
  sync, whitespace, manifest scope and fresh ordinary/high review.
- RED: not applicable; this investigation is docs-only and adds no executable behavior.
- Prohibited: history, full baseline, affected execution/benchmark, live
  matrix, v15 successor creation, certification and terminal v14 reuse.
- GREEN: exact local/remote safe base, terminal chronology/forensic boundary,
  origin/mutant/scheduler/selector contract and successor-absence checks
  passed; delta/main sync is byte-exact; strict OpenSpec passed `24/24` before
  archive and `23/23` after; JSON/TOML, source classification, current-only public scan `1631/0`,
  whitespace and manifest scope passed with production/test/runtime LOC `0`.
- NOT RUN by contract: reachable history, real full/affected execution or
  benchmark, live matrix and certification checks.

## Archive
- `openspec/changes/archive/2026-08-29-investigate-affected-release-profile-origin-proof-discrimination-v15/`

## Related
- `openspec/changes/investigate-affected-release-profile-origin-proof-discrimination-v15/`
- `openspec/board/4.done/authorize-bounded-affected-release-profile-v14.md`
- `openspec/board/4.done/investigate-affected-release-profile-exhaustive-guard-closure-v14.md`
- `openspec/specs/changerail-release-ci/spec.md`

## Result
DO complete: one clean docs-only v15 investigation is synchronized and archived
with independent source-authored origins, semantic-mutant uniqueness, closed
scheduler ownership and exact owner-discriminating selector proof.
Production/test/runtime LOC remain `0`; successors and prohibited evidence
remain absent; awaiting fresh ordinary/high review before publish.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `investigate-affected-release-profile-origin-proof-discrimination-v15`

### Why
Fresh v14 cycle 2 закрыл timeout и bounded scheduler failure, но доказал, что
pre-import fake может стать self-frozen expected origin, а inflated/reused
mutant catalog не различает scheduler alias и потерю selector operands.

### Goal
Опубликовать одну clean docs-only decision, задающую независимый canonical
origin resolver и source mutations, которые обязаны менять exact public
selection/activation behavior.

### Scope
- this card;
- same-slug OpenSpec artifacts;
- synchronized `changerail-release-ci` specification and archive metadata.

### Acceptance
- Все acceptance criteria карточки observable и machine-checkable без чтения
  terminal v14 payload или запуска prohibited release/certification evidence.

### Depends On
- `authorize-bounded-affected-release-profile-v14`
- `investigate-affected-release-profile-exhaustive-guard-closure-v14`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`

### Related
- `openspec/changes/investigate-affected-release-profile-origin-proof-discrimination-v15/`

## Log
- 2026-08-29 created in a clean worktree and remote branch from exact safe
  published authorization v14 after terminal cycle-2 `NO-GO`; only validated
  findings/counters crossed the boundary and no v14 executable payload/evidence
  was imported.
- 2026-08-29 FF created one apply-ready same-slug docs-only change with six v15
  requirements, independent origin anchors and discriminating scheduler/
  selector proof; strict target/all and whitespace checks passed while v15
  executable successors and prohibited evidence remained absent.
- 2026-08-29 DO synchronized six release-CI requirements byte-exactly,
  archived the same-slug change and passed strict OpenSpec, JSON/TOML, source
  classification, current-only public scan and whitespace with zero executable
  LOC; prohibited history/release/live/certification checks were not run.
- 2026-08-29 fresh ordinary/high review cycle 1 returned `NO-GO` on two
  docs-only consistency blockers: case-to-mutant cardinality and explicit
  pre-import token coverage; the sole bounded same-card repair aligned card,
  design and normative spec without executable changes.
- 2026-08-29T05:33:42Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.
