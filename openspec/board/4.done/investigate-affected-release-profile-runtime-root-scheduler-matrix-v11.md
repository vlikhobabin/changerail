# Исследовать runtime-root и scheduler-matrix boundary affected profile v11

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
archived

## Series
- release-baseline-acceleration

## Series Index
- 28

## Source
- Latest safe published affected-profile reference: exact authorization v10
  commit `fb43b46fdc4a746eabdc8325c231f4724daccb44`.
- Terminal unpublished `implement-bounded-affected-release-profile-v10`
  completed review cycle 1 with four blockers and one major finding, consumed
  its sole bounded repair, then completed fresh cycle 2 with `7/9` acceptance,
  two blockers and rescue budget `1/1/0` exhausted.
- Only validated verdict summaries and counters cross this boundary. Terminal
  v10 card, code, tests, specs, manifest, logs and raw evidence are forensic-only
  and MUST NOT be read, copied, cherry-picked or accepted by this clean lineage.

## Summary
Опубликовать docs-only investigation/design decision до любого executable v11:
замкнуть exact dedicated runtime-root admission до scheduler mutation и задать
конечную исчерпывающую scheduler summary/row cross-field proof matrix.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`
- Lineage escalation: runtime containment и complete connected proof остались
  blockers после единственного v10 repair; эта карточка является обязательной
  investigation/design остановкой, а не ещё одним executable patch.
- Independent review: one fresh `gpt-5.6-sol`/`high`
- Same-card repair budget limit/used/remaining: `1/0/1`, exhausted `false`

## Depends On
- `authorize-bounded-affected-release-profile-v10`
- `rescue-affected-release-profile-runtime-containment-dynamic-execution-boundary-v10`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`

## Blocks
- `authorize-bounded-affected-release-profile-v11`
- `implement-bounded-affected-release-profile-v11`
- `certify-accelerated-release-loop-v1`

## Acceptance
- Decision создаётся и публикуется docs-only из exact safe authorization v10
  commit `fb43b46f...`; terminal v10 payload/evidence не импортируются, а
  concise chronology сохраняет cycle 1, sole repair, cycle 2 `NO-GO` и
  exhausted budget `1/1/0`.
- Decision объясняет repeated-defect escalation: v10 больше не исправляется и
  не публикуется; до отдельной v11 authorization запрещены implementation card,
  focused test, production, CI и main-spec executable mutations.
- Runtime-root contract до Git, scheduler и filesystem mutation отклоняет
  empty value, `.`, repository root, NUL, surrogate/non-encodable и overlong
  leaf, lexical/real escape, symlink, wrong type/access и non-dedicated target;
  missing leaf допустим только как bounded direct child real writable parent.
- Decision задаёт read-only pre-reservation proof для каждого выбранного task
  root: exact bounded token, direct-child containment, отсутствие существующего
  leaf/symlink/conflict и единый aggregate failure до `run_plan`; race и
  reservation failure остаются bounded fail-closed и не дают authority.
- Scheduler proof matrix перечисляет каждый допустимый reason tuple
  (`completed`, terminal, outer, synthetic, `cancelled`), top-level fields,
  version/jobs/status/size, identity/order/count и для каждого row field —
  exact valid neighbors и one-field invalid type/bound/cross-field cases.
- Future connected catalog не выводит completeness из собственного неполного
  списка: нормативный requirement-to-row map и immutable reason schema являются
  независимыми источниками; missing/extra/duplicate/reused row, masked fault,
  непроверенный valid tuple или one-field mutant fail closed через public
  `profile.main`/`run_smoke` без replacement production functions.
- Decision определяет единственный дальнейший порядок: эта investigation,
  docs-only `authorize-bounded-affected-release-profile-v11`, clean
  `implement-bounded-affected-release-profile-v11`, затем certification.
  Future authorization связывает exact successor и ceiling не выше 500 строк;
  future implementation добавляет не более 499 production LOC.
- Investigation добавляет production/test/runtime LOC `0`, изменяет только
  свою card, same-slug OpenSpec artifacts, synchronized release-CI spec и
  archive metadata; v11 authorization/implementation и certification остаются
  отсутствующими.
- History, real full baseline, affected execution/benchmark, live matrix и
  certification checks не запускаются и не принимаются.

## Change Set
- `investigate-affected-release-profile-runtime-root-scheduler-matrix-v11`

## Verify
- Required: exact safe base/remote, terminal-v10 concise chronology and forensic
  boundary, repeated-defect escalation, runtime-root/pre-reservation decision,
  exhaustive scheduler matrix, future order/ceiling, strict OpenSpec,
  JSON/TOML, source classification, current-only public scan, archive/main sync,
  whitespace, manifest scope and fresh ordinary/high review.
- RED: not applicable; this investigation is docs-only and adds no executable behavior.
- Prohibited: history, full baseline, affected execution/benchmark, live matrix,
  v11 authorization/implementation creation, certification and terminal v10 reuse.
- GREEN: exact base/remote and focused contract/absence oracle passed; strict
  OpenSpec passed `23/23` after archive; JSON/TOML parsing, source
  classification, current-only public scan `1595/0`, archive/main exact sync
  and whitespace checks passed with production/test/runtime LOC `0`.
- NOT RUN by contract: reachable history, real full/affected execution or
  benchmark, live matrix and certification checks.

## Archive
- `openspec/changes/archive/2026-08-28-investigate-affected-release-profile-runtime-root-scheduler-matrix-v11/`

## Related
- `openspec/changes/archive/2026-08-28-investigate-affected-release-profile-runtime-root-scheduler-matrix-v11/`
- `openspec/board/4.done/authorize-bounded-affected-release-profile-v10.md`
- `openspec/board/4.done/rescue-affected-release-profile-runtime-containment-dynamic-execution-boundary-v10.md`
- `openspec/specs/changerail-release-ci/spec.md`

## Result
DO complete: one docs-only v11 investigation decision is synchronized and
archived with exact dedicated runtime-root/pre-reservation admission and an
independently enumerable scheduler matrix. Production/test/runtime LOC remain
`0`; successors and prohibited evidence remain absent; awaiting fresh
ordinary/high review before publish.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `investigate-affected-release-profile-runtime-root-scheduler-matrix-v11`

### Why
Fresh v10 cycle 2 proved that connected mutation mechanics alone do not close an
omitted runtime-root boundary or establish the accumulated exhaustive scheduler
cross-field matrix; exhausted-budget policy forbids another v10 patch.

### Goal
Publish one clean docs-only decision that makes the complete v11 runtime and
scheduler proof boundary independently enumerable before authorization.

### Scope
- this card;
- same-slug OpenSpec artifacts;
- synchronized `changerail-release-ci` specification and archive metadata.

### Acceptance
- All card acceptance criteria above are observable and machine-checkable.

### Depends On
- `authorize-bounded-affected-release-profile-v10`
- `rescue-affected-release-profile-runtime-containment-dynamic-execution-boundary-v10`
- `decide-accelerated-release-loop-integration-boundary`
- `implement-bounded-release-semantic-scheduler-v1`

### Related
- `openspec/changes/investigate-affected-release-profile-runtime-root-scheduler-matrix-v11/`

## Log
- 2026-08-28 created in a clean worktree from exact safe published v10
  authorization after validated terminal implementation-v10 cycle-2 `NO-GO`;
  only concise findings/counters crossed the forensic boundary.
- 2026-08-28 FF created one apply-ready same-slug docs-only change with exact
  dedicated runtime-root/pre-reservation and independent scheduler-matrix
  contracts; strict target validation and whitespace checks passed, while v11
  executable successors and prohibited release evidence remained absent.
- 2026-08-28 DO synchronized five release-CI requirements, archived the
  same-slug change and passed exact base/contract/absence, strict OpenSpec,
  JSON/TOML, source-classification, current-only public-surface and whitespace
  checks with production/test/runtime LOC `0`; no prohibited release or
  certification execution ran.
- 2026-08-28T08:10:01Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.
