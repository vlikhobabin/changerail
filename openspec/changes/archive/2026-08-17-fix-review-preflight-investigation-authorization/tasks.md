## 1. Deterministic Gate

- [x] 1.1 Exclude Go `*_test.go` paths from production LOC classification.
- [x] 1.2 Parse and fail-closed validate the bounded published-investigation
  authorization, then apply it only to the LOC/protocol complexity decisions.
- [x] 1.3 Extend the preflight-result schema with the authorization state.

## 2. Contract And Regression Coverage

- [x] 2.1 Add adversarial smoke fixtures for valid, missing, stale,
  mismatched and over-ceiling authorization plus Go test LOC exclusion.
- [x] 2.2 Update only the targeted public contract, methodology, skills and
  card templates required to declare the structured authorization.

## 3. Verification And Archive

- [x] 3.1 Run focused preflight/schema smoke, Python compile/lint, strict
  OpenSpec validation, public-surface scan, diff check and release baseline.
- [x] 3.2 Sync delta specs, archive the change and leave its card ready for one
  fresh independent ordinary `high` review.
