## 1. Replacement Decision

- [x] 1.1 Confirm that the card, proposal, design and delta preserve both
  cycle-2 blockers, classify malformed history framing as a repeated unresolved
  invariant and keep the exhausted payload forensic-only.
- [x] 1.2 Confirm the exact `ls-tree -z` and raw-tree grammars, including
  `raw_name` as exactly one non-empty strict-UTF-8 slash-free Git tree
  component with no NUL, control/DEL, backslash, `.` or `..`, the single
  persistent raw-tree batch hypothesis and fail-closed behavior before cache
  lookup, reuse or successful output.
- [x] 1.3 Confirm that tracked changes are limited to board/OpenSpec/spec
  relationship documentation and that production additions relative to
  `ccccb62562e1646b595119edd3326763860f14a7` are exactly `0` LOC.

## 2. Successor And Benchmark Contract

- [x] 2.1 Bind only `deliver-path-sensitive-public-history-scan-replacement`
  to exact safe production baseline `ccccb62562e1646b595119edd3326763860f14a7`,
  total added production ceiling `<=300` LOC and same-card repair/rescue budget
  `0`.
- [x] 2.2 Freeze `history-fixture-v1` fingerprint and scale, exact legacy blob,
  trial order, timed process boundary, CV replacement rule, unrounded ratios,
  memory bounds and cold/warm thresholds without importing forensic evidence.
- [x] 2.3 Preserve the complete successor verification floor and make
  `parallelize-isolated-release-smoke-cases` depend on the published replacement
  rather than the exhausted implementation or this decision alone.
- [x] 2.4 Require a connected successor negative fixture proving that a
  slash-bearing raw-tree name fails closed before traversal output, cache
  lookup, reuse, partial findings or successful history result.

## 3. Decision Verification

- [x] 3.1 Run `bin/openspec validate
  decide-path-sensitive-public-history-scan-replacement --strict` and
  `bin/openspec validate --all --strict`.
- [x] 3.2 Parse `.mcp.json` with `python3 -m json.tool`, parse
  `.codex/config.toml` with `tomllib` and run
  `python3 scripts/public-surface-scan.py` without `--history`.
- [x] 3.3 Run `bin/changerail-source-classification --workspace . --json check`,
  derive and scope-check the decision delivery manifest, and run deterministic
  review preflight on the exact archived decision fingerprint.
- [x] 3.4 Run `git diff --check` plus an explicit no-index whitespace scan for
  every untracked file, confirm the scoped diff/status, and do not run the
  history scan, frozen benchmark or full release baseline for this decision.
