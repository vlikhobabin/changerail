## 1. Decision-Only Contract

- [x] 1.1 Сверить card и artifacts: один universal strict decoder покрывает
  source `Investigation authorization` и inline
  `Published investigation authorization`, сохраняя published pair/type order.
- [x] 1.2 Зафиксировать numeric lexeme 64, per-value 4096 chars/255 tokens/depth
  16 и aggregate 16 fields/16384 chars/1020 tokens вместе с exact stable
  details и complete deterministic first-failure order: field count, aggregate
  chars, per-value chars, strict constants, syntax, numeric length, depth,
  per-value tokens, aggregate tokens.
- [x] 1.3 Зафиксировать source/inline cardinality, duplicate/shape/identity
  details, typed mapping boundary и zero semantic/review/model dispatch oracle.
- [x] 1.4 Зафиксировать connected production `run_preflight` matrix при
  `PYTHONINTMAXSTRDIGITS=640` и `0`: numeric signs/fractions/exponents,
  `NaN`/`Infinity`, exact/over resource boundaries, both roles и existing
  type-safe/phase-routed rows, включая observable same-event collision rows.
- [x] 1.5 Зафиксировать единственную replacement identity
  `authorize-total-bounded-phase-routed-resume-integrity-payload`, exact
  source/reference objects, reciprocal relations, ceiling 500, protocol true и
  future parser/smoke ceiling 300.
- [x] 1.6 Подтвердить scoped absence production parser, smoke/tests, schemas,
  runner, CLI, runtime docs, failed payload, replacement/successor cards и
  runtime verdict/evidence.
- [x] 1.7 Cycle-1 same-card repair R1: сохранить original `NO-GO`, определить
  single-pass tie precedence и добавить source/inline collision matrix для
  `PYTHONINTMAXSTRDIGITS=640` и `0` без изменения limits, details или lineage.

## 2. Spec Sync And Archive

- [x] 2.1 Только во время apply синхронизировать delta requirement `Total bounded
  authorization JSON decoder decision` в
  `openspec/specs/changerail-contracts/spec.md`.
- [x] 2.2 После successful decision validation архивировать
  `decide-total-bounded-authorization-json-decoder-boundary` и обновить только
  investigation card archive/result/related metadata.
- [x] 2.3 Оставить card в `3.inprogress` для fresh independent review; `GO`
  производится review и требуется только publish/finalization.
- [x] 2.4 Не создавать replacement authorization до publication investigation и
  не создавать downstream successor до publication replacement source.

## 3. Investigation Verification

- [x] 3.1 Запустить `bin/openspec validate
  decide-total-bounded-authorization-json-decoder-boundary --strict` до sync.
- [x] 3.2 После apply-time sync запустить `bin/openspec validate
  changerail-contracts --strict` и `bin/openspec validate --all --strict`.
- [x] 3.3 Запустить `python3 scripts/public-surface-scan.py`,
  `python3 -m json.tool .mcp.json` и TOML parse `.codex/config.toml`.
- [x] 3.4 Запустить `git diff --check`, explicit whitespace scan всех untracked
  artifacts и scoped status/diff check для allowed card/change/main-spec/archive
  paths.
- [x] 3.5 Записать runtime RED/GREEN как `not applicable` для decision-only
  payload; не создавать synthetic runtime evidence.

## 4. Later Implementation Handoff

- [x] 4.1 В replacement card потребовать separate connected production
  `run_preflight` matrix при `PYTHONINTMAXSTRDIGITS=640` и `0` с exact identical
  serialized results и production-owned counters.
- [x] 4.2 Потребовать existing type-safe и phase-routed resume-integrity matrices,
  полный `python3 scripts/smoke-review-preflight.py`,
  `python3 scripts/run-release-baseline.py` и
  `bin/changerail-source-classification check --json`.
- [x] 4.3 Потребовать strict OpenSpec, `python3 -m py_compile` changed Python,
  `ruff check` when available, public scan, `git diff --check` и scoped diff
  checks; planning checks этой investigation не засчитывать как runtime proof.

## 5. Cycle-1 Rescue Verification

- [x] 5.1 Запустить strict OpenSpec, public-surface scan, JSON/TOML parse,
  tracked/untracked whitespace, allowed-scope и archive/main requirement exact
  equality checks для frozen repair payload.
- [x] 5.2 Обновить ignored manifest, evidence и non-normalizing preflight для
  same-card rescue attempt 1; не писать verdict или `GO` и сохранить cycle-1
  `NO-GO` как canonical review input.
