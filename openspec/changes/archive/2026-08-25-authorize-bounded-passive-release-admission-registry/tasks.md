## 1. Publish Exact Authorization Source

- [x] 1.1 Preserve exactly one inline `Investigation authorization` object on
  the source card with six generic fields, exact rescue and future A1
  ids/canonical paths, ceiling `500` and protocol flag `false`.
- [x] 1.2 Confirm reciprocal documentation: rescue blocks authorization and
  future A1, authorization depends on rescue and blocks future A1, and the
  future-A1 contract requires dependency on rescue plus the exact two-field
  inline source reference.
- [x] 1.3 Keep future A1 card/code absent until this source is remote-reachable
  in `4.done`; do not reuse forensic Scope A or modify production code, tests,
  schemas, workflows, CLI, entrypoints, receipts or runtime state.

## 2. Synchronize Passive Ownership Contract

- [x] 2.1 Sync the `changerail-release-ci` delta that binds exact A1 passive
  registry/admission/selector/AST-fault ownership and excludes A2 authority,
  credentials, mutation, live access and terminal receipt behavior.
- [x] 2.2 Preserve structural dormancy: before exact published A2 no
  authoritative surface imports, invokes or activates A1; after A2 only exact
  A2 may do so and the negative-wiring oracle rejects every other path.
- [x] 2.3 Preserve future A1 limit `<=499` added production LOC against the
  exact published authorization HEAD and treat protocol `false` as a hard
  prohibition on new authority or wire protocol.

## 3. Verify And Archive In DO

- [x] 3.1 Run strict target-change, `changerail-release-ci` after sync and all
  OpenSpec validation.
- [x] 3.2 Assert the exact six-field source, exact two-field future reference,
  reciprocal relations, closed A1 ownership, structural dormancy and absence
  of future A1 card/code.
- [x] 3.3 Parse `.mcp.json` as JSON and `.codex/config.toml` as TOML; run only
  current public-surface scan and source classification.
- [x] 3.4 Run tracked and explicit-untracked whitespace checks, derive/scope
  the ignored manifest and run normalized ordinary/high review preflight.
- [x] 3.5 Confirm production/test/runtime additions are `0` LOC and do not run
  or accept history scan, full baseline, authority receipt or terminal capture
  evidence.
- [x] 3.6 Sync and archive exactly this same-slug docs-only change only after
  the cheap/current gate is GREEN; leave the card ready for independent review
  and scoped publication.
