## 1. Publish Exact S Authorization Source

- [x] 1.1 Preserve exactly one inline `Investigation authorization` object on
  the source card with six generic fields, exact published rescue and future-S
  ids/canonical paths, ceiling `500` and protocol flag `true`.
- [x] 1.2 Confirm reciprocal documentation: rescue blocks authorization and
  future S, authorization depends on rescue and blocks future S, and future S
  requires the exact two-field published-source reference.
- [x] 1.3 Keep future S card/code absent until this source is remote-reachable
  in `4.done`; do not modify production code, tests, schemas, workflows, CLI,
  entrypoints, registries, receipts or runtime state.

## 2. Synchronize Child-Supervisor Boundary

- [x] 2.1 Sync the `changerail-release-ci` delta that limits future S to the
  platform-neutral child protocol and POSIX hard stdout/stderr/report/process/
  deadline/TERM-KILL/reap/subreaper boundary.
- [x] 2.2 Preserve exclusions for Git parsing, scheduler policy, Windows Job
  behavior, registry, baseline/CI activation and receipt ownership, plus the
  absence of credential, mutation and live authority.
- [x] 2.3 Preserve `<=499` added production LOC against the exact published
  authorization HEAD and structural dormancy until only exact published A3 may
  activate S.

## 3. Verify And Archive In DO

- [x] 3.1 Run strict target-change, `changerail-release-ci` after sync and all
  OpenSpec validation.
- [x] 3.2 Assert exact six-field source, exact future two-field reference,
  reciprocal relations, allowed/excluded S ownership, A3 dormancy and absence
  of future S card/code.
- [x] 3.3 Parse `.mcp.json` as JSON and `.codex/config.toml` as TOML; run only
  current public-surface scan and source classification.
- [x] 3.4 Run tracked and explicit-untracked whitespace checks, derive and
  scope the ignored delivery manifest, and run normalized ordinary/high
  preflight after archival handoff.
- [x] 3.5 Confirm production/test/runtime additions are `0` LOC and do not run
  or accept history scan, full baseline, live execution, review, commit or push
  evidence.
- [x] 3.6 Sync and archive exactly this same-slug docs-only change only after
  the focused/current gate is GREEN; leave the card in `3.inprogress` for
  independent review.
