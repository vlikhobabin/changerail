## 1. Publish Exact S2 Authorization Source

- [x] 1.1 Put exactly one six-field `Investigation authorization` object on
  the source card with the published S2 decision, exact future implementation,
  ceiling `500` and protocol flag `true`.
- [x] 1.2 Record reciprocal relations: the decision blocks authorization and
  implementation; this authorization depends on the decision and blocks only
  implementation; the future implementation depends on both and uses only the
  exact two-field published authorization reference.
- [x] 1.3 Keep the future successor card/code absent and production, test and
  runtime LOC at `0`; do not reuse S v1 or failed unpublished material.

## 2. Synchronize the Bounded S2 Contract

- [x] 2.1 Synchronize `psutil==7.1.0` in runtime/development/bootstrap/admission,
  bounded `selectors`/`prctl` and writable-cgroup prohibition.
- [x] 2.2 Preserve distinct positive deadlines, exact total arithmetic,
  terminal cleanup, fail-closed psutil errors, `(pid, create_time)`, inclusive
  128/128/32 maxima and second-empty success threshold.
- [x] 2.3 Preserve dormant baseline/CI/review/receipt/entrypoint wiring and
  downstream H4/I3/W1/R3/A3 blocking until publication plus later refresh.

## 3. Verify and Archive Docs-Only Delivery

- [x] 3.1 Run strict target, synchronized `changerail-release-ci` and all
  OpenSpec validation.
- [x] 3.2 Assert exact object/relations, future-reference/absence, pin,
  adapter/cgroup, deadline, identity, cap, stable-empty, dormancy and
  downstream-block contracts.
- [x] 3.3 Parse `.mcp.json` as JSON and `.codex/config.toml` as TOML; run
  current-only public-surface scan, source classification and tracked plus
  explicit-untracked whitespace checks.
- [x] 3.4 Derive and scope the ignored delivery manifest, run normalized
  ordinary/high preflight after archive handoff, and leave the card in
  `3.inprogress` for independent review.
- [x] 3.5 Do not run or accept history scan, full release baseline, live
  execution, successor, review, commit or push evidence.
