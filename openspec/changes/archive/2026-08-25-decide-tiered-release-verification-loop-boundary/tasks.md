## 1. Finalize Decision Contract

- [x] 1.1 Confirm the decision changes only this card and one docs-only
  OpenSpec change, preserves the terminal successor as forensic-only, and adds
  zero production, test or runtime LOC.
- [x] 1.2 Verify the canonical 35-ID newline inventory and exact SHA-256
  `7147ee3c4b067486162f3dc1fee218c87eb40cbdb0d7730a9a78442da7986513`,
  with one owner per ID and exactly six Windows matrix leaves.
- [x] 1.3 Verify toolchain admission precedes every semantic child and checks
  usable Python/runtime/dev pins, `ruff 0.6.9`, Git/workspace and pinned
  OpenSpec `1.3.1` through Node/npm/npx paths.
- [x] 1.4 Verify bounded Windows jobs/isolation/timeout/output/reap behavior,
  jobs-1/default parity, removal of exactly four duplicate processes and strict
  local/live separation.
- [x] 1.5 Verify affected A/M/D/R/C/untracked/multi-area selection, declared
  bounds, minimum floor and full fallback for unknown/ambiguity/self-change;
  requested affected authority remains false after full fallback.

## 2. Bind Ordered Successors

- [x] 2.1 Preserve the exact tiered authorization object with ceiling `500`,
  protocol allowance `true`, successor
  `implement-tiered-release-verification-loop` and independent `<=499`
  production LOC limit against
  `45a2de98924c61bb9e944767013ea09918bba4b0`.
- [x] 2.2 Preserve the separate post-tiered scanner-v2 authorization with
  ceiling `301`, protocol allowance `false`, successor
  `deliver-clean-git-compatible-structural-history-scan-v2` and independent
  `<=300` production LOC limit against exact published tiered HEAD.
- [x] 2.3 Preserve the post-tiered `verify-project` authorization with ceiling
  `501`, protocol allowance `false`, successor
  `parallelize-isolated-verify-project-cases` and `<=500` production LOC limit
  against exact published tiered HEAD; retain independent scanner-v2 order and
  the existing review/delivery smoke successor.
- [x] 2.4 Confirm neither authorization nor implementation card/code exists in
  this payload and each future executable successor has one predeclared
  terminal full-release capture without retry.

## 3. Synchronize Release-CI Decision

- [x] 3.1 Sync the four complete MODIFIED requirements and two ADDED
  requirement into `openspec/specs/changerail-release-ci/spec.md` without
  duplicate headings or lost scenarios.
- [x] 3.2 Run a heading-slice ownership oracle proving modified baseline
  coverage has 4 scenarios, Windows entrypoint/wiring/matrix each have 2, and
  the tiered requirement has exactly 12 scenarios and the `verify-project`
  requirement has exactly 5 scenarios owned by their requirements.
- [x] 3.3 Archive
  `decide-tiered-release-verification-loop-boundary` only after explicit sync,
  using duplicate-safe `--skip-specs` when the normal archive gate reports the
  already-synced requirement diagnostic.

## 4. Verify Docs-Only Payload

- [x] 4.1 Run strict target, `changerail-release-ci` capability and all-change
  OpenSpec validation.
- [x] 4.2 Parse `.mcp.json` with `python3 -m json.tool` and
  `.codex/config.toml` with Python `tomllib`.
- [x] 4.3 Run `python3 scripts/public-surface-scan.py` current-only and
  `bin/changerail-source-classification --workspace . --json check`; do not run
  history scan, benchmark or `scripts/run-release-baseline.py`.
- [x] 4.4 Run `git diff --check` plus an explicit whitespace check for every
  untracked card/artifact, then confirm scoped manifest/status and zero
  production/test/runtime additions.
- [x] 4.5 Run normalized critical preflight and hand the archived payload to a
  fresh independent Sol/`xhigh` review. Allow at most one scoped Terra/high
  repair followed by fresh Sol/`xhigh` re-review; do not review, commit or push
  during planning or DO.
