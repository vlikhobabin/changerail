## 1. Record S2 Decision Lineage

- [x] 1.1 Put the exact six-field future authorization object on the same-slug
  decision card; block the future authorization and implementation cards.
- [x] 1.2 Record the reciprocal future dependencies: authorization depends on
  this decision and blocks only S2; S2 depends on both sources and uses only
  its exact two-field published authorization reference.
- [x] 1.3 Preserve both published prerequisites, state that S v1's one-
  successor authorization cannot authorize v2, and retain failed unpublished
  episodes as generic forensic input only.

## 2. Synchronize the Bounded S2 Contract

- [x] 2.1 Add the complete `changerail-release-ci` delta for
  `psutil==7.1.0` pins in runtime/development/bootstrap/admission, bounded
  `selectors`/`prctl`, and the writable-cgroup prohibition.
- [x] 2.2 Define separate positive execution and cleanup timeouts, exact
  `execution_timeout + cleanup_timeout + 1.0s` total budget, terminal cleanup,
  fail-closed psutil errors, `(pid, create_time)` identity, inclusive allowed
  128/128/32 maxima, strict `>128`/`>128`/`>32` terminal excesses and a second
  consecutive empty scan as the stable-empty success threshold.
- [x] 2.3 Add structural dormancy, downstream H4/I3/W1/R3/A3 refresh blocking
  and the connected static proof matrix without baseline or CI activation.

## 3. Verify and Archive Docs-Only Delivery

- [x] 3.1 Run strict target-change, synchronized `changerail-release-ci` and
  all OpenSpec validation.
- [x] 3.2 Assert the exact object and reciprocal relations, old/failed
  authorization non-reuse, all pin/supervision/timeout/cleanup caps, dormant
  downstream block, negative rejection of premature stable-empty success and
  strict cap excess, no future cards/code and production/test/runtime LOC `0`.
- [x] 3.3 Parse `.mcp.json` as JSON and `.codex/config.toml` as TOML; run
  current-only public-surface scan, source classification and tracked plus
  explicit-untracked whitespace checks.
- [x] 3.4 Derive and scope the ignored delivery manifest, run normalized
  ordinary/high preflight after archival handoff, synchronize and archive only
  this same-slug docs-only change, and leave the card in `3.inprogress` for
  independent review.
- [x] 3.5 Do not run or accept history scan, full release baseline, live
  execution, review, commit or push evidence.
