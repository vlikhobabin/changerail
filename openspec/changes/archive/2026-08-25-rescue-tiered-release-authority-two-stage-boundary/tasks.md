## 1. Зафиксировать two-stage decision

- [x] 1.1 Синхронизировать release-CI contract с exact A1/A2 authorization
  objects, disjoint ownership, time-bounded A1 dormancy, exclusive A1
  publication gate и запретом reuse failed Scope A payload.
- [x] 1.2 Обновить карточку exact reciprocal lineage, dormancy proof boundary,
  A1 prohibition history/full/receipt/capture, A2 atomic one-shot policy и
  immutable downstream order.

## 2. Проверить docs-only payload

- [x] 2.1 Проверить strict target/capability/all OpenSpec validation и exact
  six-field A1/A2 objects, paths, IDs, ceilings `500` и flags `false`/`true`.
- [x] 2.2 Проверить ownership oracle: A1 passive and dormant до published A2,
  затем activated только exact A2; A1 publication gate допускает только
  offline/focused/static/current proof и запрещает history/full/receipt/capture;
  A2 owns sole terminal activation and atomic capture
  `implement-terminal-release-authority-activation-cycle-1` с budget `0/0/0`.
- [x] 2.3 Проверить source/lineage/order только от published `25f756e` и
  `0fba407`, forensic exclusion, executable LOC `0`, JSON/TOML, current-only
  public scan, classification, whitespace, scoped manifest и preflight.
- [x] 2.4 Архивировать ровно один same-slug docs-only change после sync; не
  создавать successors и не запускать history scan или full baseline.

## 3. Same-card repair after cycle-1 NO-GO

- [x] 3.1 Исправить только R1-R3: time-bound A1 dormancy through published A2,
  exclusive prohibition of inapplicable A1 authority evidence и truthful
  ordered log without invented timestamps.
- [ ] 3.2 Выполнить fresh cycle-2 preflight и independent Sol/xhigh re-review;
  ignored manifest/verdict остаются pending до этого gate.
