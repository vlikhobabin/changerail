## 1. Зафиксировать split decision

- [x] 1.1 Синхронизировать card/design/spec так, чтобы A единолично владел
  admission, 35-ID registry/digest, affected/full authority, atomic capture,
  receipts/schema/pub gate и canonical CI/parser oracles, не меняя Windows
  scheduling/process topology.
- [x] 1.2 Зафиксировать B как единственного владельца six-case registry,
  jobs/isolation/order, central process-group cleanup, scheduler fault matrix,
  six-ID owner transition и удаления ровно четырех standalone processes без
  права менять A registry/selector/receipt authority.
- [x] 1.3 Проверить exact six-field A/B authorization objects с ceiling `500`,
  protocol allowance `true`, exact paths/ids и независимым `<=499` production
  LOC limit против exact опубликованной authorization base каждого successor.
- [x] 1.4 Зафиксировать обязательный remote-reachable порядок A, clean
  scanner-v2, B, verify-project, затем review-preflight/delivery-runner smokes;
  broad unpublished implementation и все его payload/evidence оставить
  forensic-only.

## 2. Синхронизировать и архивировать docs-only change

- [x] 2.1 Выполнить `bin/openspec validate rescue-tiered-release-verification-split-boundary --strict`, синхронизировать delta в
  `openspec/specs/changerail-release-ci/spec.md` и подтвердить exact scenario
  ownership без потери неизмененных scenarios.
- [x] 2.2 Архивировать только
  `rescue-tiered-release-verification-split-boundary`, обновить target card и
  подтвердить production/test/runtime LOC `0`; не создавать authorization или
  implementation successors.

## 3. Выполнить cheap current-only verification

- [x] 3.1 Выполнить strict target/capability/all OpenSpec validation и
  machine-check exact A/B JSON, reciprocal future lineage, ordering,
  comparison-base/LOC and `0/0/0` executable capture policy assertions.
- [x] 3.2 Выполнить JSON/TOML parsing, current-only
  `python3 scripts/public-surface-scan.py`, classification/scope/manifest,
  `git diff --check`, ignored-status и normalized critical Sol/`xhigh`
  review-preflight checks.
- [x] 3.3 Подтвердить, что history scan, benchmark, full baseline, successor
  creation, executable payload, runtime state, review, commit и push не
  выполнялись в docs-only DO handoff.
