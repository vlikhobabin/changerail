# Proposal: bounded affected release profile v4 authorization

## Why
The published proof-connectivity decision requires a separate docs-only
authorization before the sole clean v4 implementation may be created.

## What Changes
- Bind one exact six-field investigation authorization object.
- Bind the exact two-field future implementation reference and closed lineage.
- Preserve the resolved-base and protocol non-authority mutation oracles.
- Preserve the exact v3 admission, selector, scheduler, authority and CI floor.
- Keep successor code and expensive evidence dormant until publication.

## Impact
- Methodology: creates one auditable authorization edge.
- Verification: future v4 remains bounded to <=499 production LOC.
- Public surface: docs/OpenSpec only; no code, dependency, schema or runtime state.
- Consumer projects: no impact.
