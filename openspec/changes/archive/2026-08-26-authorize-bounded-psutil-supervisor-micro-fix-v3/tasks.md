## 1. Publish Exact S3 Authorization Source

- [x] 1.1 Add exactly one ordered six-field `Investigation authorization`
  object bound to the published decision, exact S3 successor, ceiling `500`
  and protocol allowance `true`.
- [x] 1.2 Record reciprocal lineage: the decision blocks authorization and
  successor; this authorization depends on the decision and blocks only the
  successor; the future successor depends on both and uses only the exact
  two-field published authorization reference.
- [x] 1.3 Keep successor card/code absent and production, test and runtime LOC
  at `0`; retain ordinary/high review routing and exact `0/0/0` budget.

## 2. Synchronize the Bounded S3 Reconstruction Contract

- [x] 2.1 Synchronize unpublished-candidate eligibility, clean
  authorization-HEAD start, unchanged authorized paths/scope and `<=499`
  added production-LOC boundary.
- [x] 2.2 Preserve source-material-only mechanical reconstruction, terminal
  verdict/history/log/receipt/manifest/evidence non-reuse and fresh connected
  R1-R7 proof.
- [x] 2.3 Preserve R7 pipe-EOF stream-state semantics, leader-terminal-or-
  `execution_timeout` completion, cleanup ordering, no added authority and
  dormant downstream refresh gate.

## 3. Verify and Archive Docs-Only Delivery

- [x] 3.1 Run strict target, synchronized `changerail-release-ci` and all
  OpenSpec validation plus whitespace checks.
- [x] 3.2 Check the exact ordered object, reciprocal lineage, absent successor,
  eligibility/reconstruction/evidence boundary, R1-R7, EOF, dormancy and
  `0/0/0` contract.
- [x] 3.3 Parse `.mcp.json` as JSON and `.codex/config.toml` as TOML; run
  current-only public-surface and source-classification scans and tracked plus
  explicit-untracked whitespace checks.
- [x] 3.4 Derive and scope the ignored delivery manifest, run normalized
  ordinary/high preflight after archive handoff, and leave the card in
  `3.inprogress` for a fresh independent review.
- [x] 3.5 Do not run or accept history scan, full release baseline, live
  execution, successor, review, commit or push evidence.
