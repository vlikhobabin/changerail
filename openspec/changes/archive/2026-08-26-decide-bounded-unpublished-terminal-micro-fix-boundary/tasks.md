## 1. Record the Exact v3 Boundary

- [x] 1.1 Record the exact future six-field authorization object, its
  decision/authorization/successor relation and the successor's exact
  two-field authorization reference.
- [x] 1.2 Require a clean authorization HEAD, same authorized production paths,
  no scope/dependency/schema/ownership expansion and `<=499` added production
  LOC for the only future v3 successor.
- [x] 1.3 Keep the authorization and successor cards/code absent, preserve S3
  dormancy and block downstream refresh pending publication.

## 2. Define Fresh Proof and R7 Semantics

- [x] 2.1 Permit mechanical reconstruction of executable code and tests only
  as source material; prohibit reuse of verdict, history, log, receipt,
  manifest and terminal evidence.
- [x] 2.2 Require all prior findings to be independently closed, exactly one
  isolated latest blocker, and fresh reruns of the connected R1-R7 proof.
- [x] 2.3 Define pipe EOF as stream state only; require observed leader
  terminal state or `execution_timeout`, then cleanup.
- [x] 2.4 Fix the v3 delivery budget at one implementation attempt, one fresh
  Sol/high review and repair/retry/rescue `0/0/0`, with no credential,
  mutation, live or final authority.

## 3. Verify and Archive Docs-Only Delivery

- [x] 3.1 Run strict change, `changerail-release-ci` and all OpenSpec
  validation after synchronizing the full release-CI delta.
- [x] 3.2 Assert exact object field counts, reciprocal order, v3 admission,
  fresh R1-R7 proof, R7 EOF semantics, dormant wiring and future-card absence.
- [x] 3.3 Parse `.mcp.json` as JSON and `.codex/config.toml` as TOML; run
  current-only public-surface scan, source classification and tracked plus
  explicit-untracked whitespace checks.
- [x] 3.4 Derive and scope the ignored delivery manifest, run normalized
  ordinary/high preflight after archive handoff, and leave the card in
  `3.inprogress` for fresh independent review.
- [x] 3.5 Do not run or accept history scan, full release baseline, live
  execution, authorization card, successor, review, commit or push evidence.
