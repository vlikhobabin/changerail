## 1. Record the Broker Ownership Boundary

- [x] 1.1 Record the exact future authorization and implementation identities,
  reciprocal lineage, six-field authorization object and two-field successor
  reference.
- [x] 1.2 Require broker subreaper readiness before target launch and forbid
  caller-global subreaper, child scanning and before/after ownership inference.
- [x] 1.3 Define the pre-existing bystander plus later descendant negative
  boundary and broker-owned target tree positive boundary.
- [x] 1.4 Exhaust the earlier v3 successor after this decision and make exact
  v4 the sole conforming future supervisor implementation lineage.

## 2. Bound Protocol, Cleanup and Delivery

- [x] 2.1 Define the closed versioned protocol, sequence and byte/message bounds,
  valid message order and exactly one terminal post-cleanup report.
- [x] 2.2 Make EOF, protocol faults, broker/identity faults, timeout and missing
  cleanup proof terminal; preserve a precise outer containment boundary.
- [x] 2.3 Require the full connected ownership, protocol, execution, cleanup,
  controller and dormancy proof matrix.
- [x] 2.4 Limit v4 to `<=499` production LOC, no new external dependency, one
  initial Sol/high review, one bounded repair and one final Sol/high re-review.

## 3. Verify and Archive Docs-Only Delivery

- [x] 3.1 Run strict target, capability and all OpenSpec validation after
  synchronizing the full delta.
- [x] 3.2 Assert exact objects, lineage, ownership/protocol/failure wording,
  proof matrix, budget, future-card absence and executable dormancy.
- [x] 3.3 Parse JSON/TOML; run current-only public scan, source classification,
  tracked/untracked whitespace, manifest validation/scope and preflight.
- [x] 3.4 Archive the one same-slug change and retain the card in
  `3.inprogress` for one fresh ordinary/high review.
- [x] 3.5 Do not run or accept history, full baseline, live matrix, successor,
  review, commit or push during FF/DO.
