## 1. Record Exclusive V5 Lineage

- [x] 1.1 Record exact decision, authorization and implementation identities,
  reciprocal dependencies/blocks, six-field object and two-field reference.
- [x] 1.2 Exhaust the unpublished v4 path without editing its published source
  cards or archived artifacts; make exact v5 exclusive.
- [x] 1.3 Freeze clean-start, no-reuse, dependency, `<=499` LOC and `0/0/0`
  one-review boundaries.

## 2. Freeze Connected Counterfactual Proof

- [x] 2.1 Require R8 public-`supervise` fatal/timeout cleanup proof and the exact
  removal mutation that MUST turn it red.
- [x] 2.2 Require R9 post-identity pidfd signaling observation and the exact
  PID-only mutation that MUST turn it red.
- [x] 2.3 Reject direct-helper substitutes, pre-signal mismatch substitutes,
  ambiguous/no-op mutations and reused evidence.
- [x] 2.4 Require fresh bounded retained evidence and dormant production wiring.

## 3. Verify and Archive Docs-Only Decision

- [x] 3.1 Validate the active change strictly before archive, synchronize the
  complete delta and validate the capability and all OpenSpec specs strictly.
- [x] 3.2 Assert exact objects, lineage, v4 closure, public-path/counterfactual
  wording, budgets, future-card absence and production/test/runtime LOC `0`.
- [x] 3.3 Parse JSON/TOML; run source classification, current public scan,
  tracked/untracked whitespace and manifest scope checks.
- [x] 3.4 Archive the same-slug change, keep the card `3.inprogress`, run
  normalized ordinary/high preflight and obtain one fresh Sol/high review.
- [x] 3.5 Do not run history, full baseline, live matrix, successor work or
  publish before a fresh GO.
