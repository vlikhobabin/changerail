## Why

Published installed-origin investigation v7 closes the remaining over-admission
defect, but executable work must remain absent until a separate authorization
binds one exact clean successor, its test-first evidence boundary and the full
connected proof floor.

## What Changes

- Publish one exact six-field authorization for the sole v7 implementation.
- Bind exact dependencies, sole block, two-field implementation reference and
  the 499-production-LOC ceiling.
- Require retained pre-production RED and independent saved-tree reconstruction.
- Require exact effective-interpreter `purelib`/`platlib` installed origins and
  production-default connected counterexamples for every non-package root.
- Preserve the repaired v6 admission, selector, scheduler, authority and CI
  floor while terminal unpublished v6 remains forensic-only.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- `changerail-release-ci`: authorize one bounded affected v7 successor under
  the published installed-origin investigation and retained RED boundaries.

## Impact

- Methodology: one clean successor becomes authorized only after publication.
- Verification: future v7 must prove exact package roots through the production
  default path and retain machine-auditable pre-production RED.
- Public surface: docs/OpenSpec only; no code, dependency, schema, CI or runtime
  state.
- Consumer projects: no impact.
