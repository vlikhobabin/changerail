## 1. Freeze Clean Investigation Lineage

- [x] 1.1 Prove local and remote rescue branch start at exact published safe
  authorization `3588c1d3de0ddc9d8ef50e81992620fc107e4e90` and retain a
  concise ignored evidence entry.
- [x] 1.2 Verify the card carries exact terminal two-cycle counts/findings,
  v5 forensic-only boundary, four predecessors, three blocks and the sole
  six-field future v6 authorization object.
- [x] 1.3 Verify authorization/implementation v6 and certification cards,
  executable payload, tests, CI and runtime authority remain absent.

## 2. Synchronize Investigation Contract

- [x] 2.1 Sync the exact v6 lineage and terminal-v5 exclusion requirements
  into `openspec/specs/changerail-release-ci/spec.md`.
- [x] 2.2 Sync runtime-output pre-filesystem admission and closed absent/
  directory/file/symlink/root/access behavior with bounded
  `semantic_started: 0` failure.
- [x] 2.3 Sync connected `MAX_PATH`, `MAX_PATHS`, per-stream and aggregate
  `MAX_GIT_BYTES` counterfactual requirements while preserving the published
  v5 RED/profile/scheduler/authority/CI proof floor.

## 3. Verify And Archive Docs-Only Decision

- [x] 3.1 Run strict change/capability/all OpenSpec validation, JSON/TOML parse,
  source classification and `git diff --check` with untracked files covered.
- [x] 3.2 Run current-only public-surface scan and prove production/test/runtime
  LOC `0`; do not run history, real full/affected, benchmark, live or
  certification checks.
- [x] 3.3 Derive and reconcile a 7-path card-owned delivery manifest and prepare
  the same-slug archive/card handoff for normalized ordinary/high preflight
  before fresh independent review.
