## 1. Lineage and decision contract

- [x] 1.1 Prove exact safe authorization v6 base and remote branch binding,
  terminal v6 `7/12` → repair → `11/12` chronology and successor absence.
- [x] 1.2 Freeze the sole rescue → authorization v7 → clean implementation v7
  → certification order and exact authorization/reference/dependency objects.

## 2. Installed-origin boundary

- [x] 2.1 Specify exact effective-interpreter `purelib`/`platlib` derivation,
  exact distribution-origin equality and fail-closed invalid-root behavior.
- [x] 2.2 Specify connected production-default happy/fault neighbors for every
  non-package `sysconfig` root, arbitrary origins, pins/versions and executable origins.
- [x] 2.3 Preserve the repaired v6 RED/profile/selector/scheduler/authority/CI/
  protocol floor and the docs-only dormant successor boundary.

## 3. Synchronize, verify and archive

- [x] 3.1 Sync the four delta requirements into
  `openspec/specs/changerail-release-ci/spec.md` idempotently.
- [x] 3.2 Run strict target/all OpenSpec, JSON/TOML, source classification,
  current-only public scan, whitespace and manifest scope checks with
  production/test/runtime LOC `0`; do not run prohibited suites.
- [x] 3.3 Archive the same-slug change, update the card/manifest and pass
  normalized ordinary/high review preflight.
