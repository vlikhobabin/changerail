## Why

Terminal affected profile v9 exhausted its only same-card repair after fresh
cycle-2 review still found runtime-output escape, dynamic execution bypasses
and an incomplete connected-proof inventory. Another executable patch would
repeat an unresolved defect class, so clean v10 needs a separately published
investigation/design boundary first.

## What Changes

- Record the concise terminal-v9 chronology and make all unpublished v9
  payload/runtime state forensic-only.
- Define strict existing/missing runtime leaf and ancestor containment before
  any Git, scheduler or filesystem mutation.
- Define a closed AST execution-ownership model that rejects direct and dynamic
  alternate import/call/raw-execution surfaces.
- Define a complete normative guard inventory whose every row has a real
  canonical neighbor, one actual-source/AST mutant, public observation and
  preceding-guard proof without replacing production functions.
- Bind the only allowed clean order to docs-only authorization v10, bounded
  implementation v10 and then final certification.

## Capabilities

### New Capabilities

- none.

### Modified Capabilities

- `changerail-release-ci`: Add the clean-v10 rescue decision, runtime
  containment, dynamic execution closure and complete connected-proof contract.

## Impact

This docs-only decision changes its board card, same-slug OpenSpec artifacts
and the accumulated `changerail-release-ci` specification. It adds no
production, test or runtime behavior and does not create authorization v10,
implementation v10 or certification artifacts.
