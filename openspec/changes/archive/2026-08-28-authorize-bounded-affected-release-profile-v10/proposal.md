## Why

Published rescue v10 closes the unresolved runtime-containment,
dynamic-execution and connected-proof design boundaries, but clean executable
work is forbidden until one separately reviewed docs-only authorization binds
the exact successor and ceiling to that decision.

## What Changes

- Publish the exact six-field investigation authorization object for the sole
  `implement-bounded-affected-release-profile-v10` successor.
- Freeze the authorization and future implementation dependency sets, sole
  blocks, direct rescue edge and two-field successor reference.
- Require retained pre-production RED and preserve the complete accumulated
  affected floor, including strict ancestor containment, closed normalized AST
  ownership and the complete public-boundary guard catalog.
- Keep implementation v10, executable payload and certification absent.

## Capabilities

### New Capabilities

- none.

### Modified Capabilities

- `changerail-release-ci`: add the exact v10 authorization and clean-successor
  contract bound to the published rescue decision.

## Impact

This docs-only change affects its board card, same-slug OpenSpec artifacts and
the accumulated `changerail-release-ci` specification. It adds no production,
test, CI, dependency, schema or runtime behavior and creates neither
implementation v10 nor certification.
