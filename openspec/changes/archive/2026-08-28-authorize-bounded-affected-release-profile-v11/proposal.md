## Why

Published investigation v11 closes the dedicated runtime-root and exhaustive
scheduler-matrix decision boundaries, but clean executable work remains
forbidden until one separately reviewed docs-only authorization binds the exact
successor and ceiling to that published decision.

## What Changes

- Publish the exact six-field investigation authorization object for the sole
  `implement-bounded-affected-release-profile-v11` successor.
- Freeze the authorization and future implementation dependency sets, sole
  blocks, direct investigation edge and two-field successor reference.
- Require retained pre-production RED and preserve the accumulated affected
  floor plus exact dedicated runtime-root, task pre-reservation and independent
  exhaustive scheduler-matrix proof.
- Keep implementation v11, executable payload and certification absent.

## Capabilities

### New Capabilities

- none.

### Modified Capabilities

- `changerail-release-ci`: add the exact v11 authorization and clean-successor
  contract bound to the published runtime-root/scheduler-matrix investigation.

## Impact

This docs-only change affects its board card, same-slug OpenSpec artifacts and
the accumulated `changerail-release-ci` specification. It adds no production,
test, CI, dependency, schema or runtime behavior and creates neither
implementation v11 nor certification.
