## Why

Terminal clean implementation v13 exhausted its sole repair while four
accumulated admission and connected-proof boundaries remained open. Another
executable patch would repeat the failed staircase; a published docs-only
investigation must first make the exact v14 closure independently enumerable.

## What Changes

- Record the concise terminal v13 chronology and make its entire unpublished
  payload and runtime evidence forensic-only.
- Freeze a bidirectional descriptor identity contract that rejects alternate
  but usable commands and targets before Git or semantics.
- Require non-following runtime-root inspection that detects dangling symlink
  leaves and every symlinked ancestor.
- Define source-bound scheduler and public selector catalogs that cannot pass
  through completed-only derivation, missing AST mutants or partial R/C cases.
- Freeze the exclusive investigation → authorization v14 → clean
  implementation v14 → certification order and bounded authorization object.
- Keep this decision docs-only with zero production/test/runtime LOC and no
  executable successors.

## Capabilities

### New Capabilities

- none

### Modified Capabilities

- `changerail-release-ci`: add the v14 investigation, forensic, exhaustive
  descriptor/runtime/scheduler/selector closure and successor-order contract.

## Impact

The tracked impact is limited to this board card, same-slug OpenSpec artifacts,
the accumulated `changerail-release-ci` specification and archive metadata.
There is no production, test, CI, dependency, schema or runtime-state change;
consumer projects are unaffected until a separately authorized future v14
implementation is published.
