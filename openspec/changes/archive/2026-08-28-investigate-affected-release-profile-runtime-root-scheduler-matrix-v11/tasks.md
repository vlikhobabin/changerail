## 1. Investigation contract

- [x] 1.1 Verify the exact published safe base/remote identity, terminal-v10
  concise chronology and forensic-only boundary without reading or importing
  terminal payload or raw evidence.
- [x] 1.2 Freeze exact dedicated runtime-root admission and aggregate read-only
  pre-reservation proof for every selected task root before Git, scheduler or
  filesystem mutation.
- [x] 1.3 Freeze the independently enumerable scheduler top-level, reason-tuple
  and row-field neighbor/mutant matrix, including bidirectional completeness
  against public observers.
- [x] 1.4 Verify the sole v11 future order, separate authorization boundary,
  499 production-LOC implementation ceiling and successor absence.

## 2. Specification lifecycle

- [x] 2.1 Synchronize the `changerail-release-ci` delta without creating v11
  authorization, implementation, focused-test or certification artifacts.
- [x] 2.2 Verify archive/main equality and archive the complete same-slug
  docs-only change.
- [x] 2.3 Update the card and ignored delivery manifest with exact scope,
  archive and concise verification handoff while retaining production, test
  and runtime LOC `0`.

## 3. Verification

- [x] 3.1 Run strict OpenSpec validation, JSON/TOML parsing, source
  classification, current-only public-surface scan and whitespace checks.
- [x] 3.2 Prove the exact base, chronology, runtime-root/pre-reservation,
  scheduler-matrix, lineage/ceiling, successor-absence and archive/main-sync
  contracts and reconcile manifest scope.
- [x] 3.3 Confirm that history, real full/affected execution or benchmark, live
  matrix and certification checks were not run, then prepare normalized
  ordinary/high preflight and require one fresh independent review before publish.
