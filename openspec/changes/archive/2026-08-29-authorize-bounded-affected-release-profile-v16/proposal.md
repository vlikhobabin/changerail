## Why

Published investigation v16 closes the public-proof and runtime-admission
boundary, but cannot itself authorize executable work. A separate remotely
published docs-only source must bind the one clean implementation v16 successor,
its ceiling and its preserved accumulated contract.

## What Changes

- Publish the exact six-field investigation authorization for only
  `implement-bounded-affected-release-profile-v16` with ceiling `500` and
  protocol allowance.
- Freeze the only future two-field reference, exact dependencies, sole
  certification block and maximum `499` production additions.
- Carry forward one physical typed registry, runtime-before-Git admission,
  independent ledgers, public real-Git selector proof, complete scheduler
  matrix and closed execution ownership.
- Preserve the original RED chronology and accumulated affected-profile floor
  solely from published sources.
- Keep authorization docs-only: implementation/test/code/CI successors and
  prohibited release/certification execution remain absent.

## Capabilities

### New Capabilities

- none.

### Modified Capabilities

- `changerail-release-ci`: add the exact docs-only authorization v16 for the
  sole clean bounded affected-profile implementation successor.

## Impact

Only the card, same-slug OpenSpec artifacts and accumulated
`openspec/specs/changerail-release-ci/spec.md` change. Production, tests, CI,
runtime protocols and publication authority do not change; terminal v15
payload and runtime evidence remain forensic-only.
