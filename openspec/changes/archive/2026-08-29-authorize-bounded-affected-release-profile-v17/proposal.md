## Why

Published investigation v17 resolved the final selector/scheduler proof
contradictions, but ChangeRail requires a separate reviewed and remotely
published authorization before any clean executable successor can exist.

## What Changes

- Bind one exact implementation v17 successor and `500` authorization ceiling
  (`499` maximum added production LOC in the successor).
- Require a new fingerprint-first retained missing-module/symbol RED before
  future executable mutation.
- Preserve the split pure `R/C 000..100` grammar versus honest real-Git
  non-zero emission proof.
- Preserve guard-relative case/requirement/mutant completeness with genuine
  canonical AST operator/operand edits.
- Carry the accumulated typed registry/admission, closed graph, authority,
  Unicode and four-step CI floor without creating implementation surfaces.

## Capabilities

### New Capabilities

- none

### Modified Capabilities

- `changerail-release-ci`: publish the exact dormant v17 authorization and
  future implementation admission contract.

## Impact

Only board/OpenSpec documentation and the accumulated release-CI specification
change. Production, tests, CI, runtime protocol and publication authority stay
unchanged; implementation v17 remains absent until authorization publication.
