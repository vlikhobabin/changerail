## Why

Published proof-oracle investigation v18 closed the repeated self-derived
scheduler, execution-graph and command-descriptor proof classes. Before any
new executable successor exists, its exact lineage and independently reviewed
35-to-30 command/operand anchor must be published in a clean docs-only commit.

## What Changes

- Authorize only `implement-bounded-affected-release-profile-v18` through the
  exact six-field investigation object and a `500` production-LOC ceiling
  (`499` maximum added production LOC).
- Publish a same-slug proof-only inventory containing 35 semantic rows, 30
  physical task commands with typed operands, non-task admission targets, a
  total ownership map and deterministic full digest.
- Require future production registry, source extraction and an independently
  implemented proof parser to match the published inventory bidirectionally.
- Preserve the v18 closed graph/mutation/connectivity/external-observation
  design, original-RED boundary and accumulated full-only-authority floor.
- Keep this authorization dormant: no production, tests, CI, implementation,
  certification or runtime authority is added.

## Capabilities

### New Capabilities

- none.

### Modified Capabilities

- `changerail-release-ci`: bind one exact affected-v18 successor and publish
  its immutable authorization-time command/typed-operand proof anchor.

## Impact

- Tracked impact is limited to this card, same-slug OpenSpec/proof artifacts,
  synchronized `changerail-release-ci` requirements and archive metadata.
- Production, test and runtime LOC remain zero; consumer commands, CI behavior,
  release authority and protocol authority do not change.
- Terminal unpublished implementation v17 artifacts and evidence remain
  forensic-only and outside this change.
