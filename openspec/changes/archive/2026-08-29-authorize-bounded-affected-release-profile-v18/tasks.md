## 1. Exact authorization lineage

- [x] 1.1 Verify exact investigation v18 local/upstream/remote base and the six-field authorization object.
- [x] 1.2 Verify exact four dependencies, sole implementation block, future two-field reference, authorization-publishing base and `499` production-LOC maximum.
- [x] 1.3 Prove implementation v18, focused tests, production, CI and certification successor artifacts remain absent.

## 2. Independent proof inventory

- [x] 2.1 Validate canonical JSONL and exact `35/30/48` section cardinalities, unique IDs, total bidirectional ownership and one task root per physical task.
- [x] 2.2 Recompute the published semantic newline digest and full length-framed SHA-256 and compare them with the checked-in values.
- [x] 2.3 Independently parse authorization-HEAD baseline commands and prove the only migration is four matrix-owned duplicate removals plus the three-operation drift group, yielding exact 30 physical rows with no semantic loss.
- [x] 2.4 Compare exact tokens, direct/embedded typed operands, requirements pins and non-task repository/toolchain/package/runtime descriptors with published contracts and authorization-HEAD source.

## 3. Dormant docs-only delivery

- [x] 3.1 Synchronize the complete `changerail-release-ci` delta and archive the same-slug change including `proof-inventory.md`.
- [x] 3.2 Run strict OpenSpec, JSON/TOML, source classification, zero executable LOC, successor absence, current-only public scan, exact sync, whitespace and manifest scope checks.
- [x] 3.3 Record RED as not applicable and confirm reachable history, full/affected execution or benchmark, live matrix and certification checks were not run.

## 4. Cycle-1 bounded review repair

- [x] 4.1 Preserve cycle-1 `NO-GO` R1 and define one closed public-safe canonicalization for authorization-HEAD `str(DRIFT_PROJECT)` without changing production source or accepting any other path normalization.
- [x] 4.2 Bind the exact AST/root derivation and repository-relative canonical token in the physical-row grammar, recompute the full digest and re-run the docs-only verification floor before fresh cycle-2 review.
