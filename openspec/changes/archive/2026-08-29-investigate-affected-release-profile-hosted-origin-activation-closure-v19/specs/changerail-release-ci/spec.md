## ADDED Requirements

### Requirement: Affected v19 investigation MUST isolate terminal v18 and close the two remaining proof gaps
ChangeRail MUST publish one clean docs-only
`investigate-affected-release-profile-hosted-origin-activation-closure-v19`
decision from exact published authorization v18 commit
`ea85f2691be7e3a7d61d0ada89fef785233da40e` after terminal unpublished v18
cycle-2 acceptance `9/11`, two blockers and exhausted repair budget `1/1/0`.
Only those validated counters and the hosted-origin/activation-graph finding
summaries MAY cross. Terminal v18 implementation card, OpenSpec, source, tests,
CI/main-spec mutation, manifest, verdicts, logs and raw evidence MUST remain
forensic-only and MUST NOT be read, copied, cherry-picked, reproduced or
accepted. Production, test and runtime LOC MUST remain zero.

#### Scenario: Exhausted v18 forces a clean design source
- **WHEN** maintainers continue the affected-profile lineage after terminal v18
- **THEN** local, upstream and remote investigation plus remote authorization references resolve to the exact published SHA
- **AND** only the validated chronology and two finding-class summaries enter tracked v19 artifacts.

### Requirement: Affected v19 hosted origin proof MUST execute the canonical setup-node toolcache branch
Future v19 MUST parse the exact source-safe four-step CI and independently
require the pinned setup-node action, `node-version: "20"`, canonical Linux
hosted layout and absence of alternate environment/execution fields before
selecting hosted origin grammar. For each of `node`, `npm` and `npx`, a separate
clean child MUST set an externally constructed real absolute disposable
`RUNNER_TOOL_CACHE` before production import, provide exactly one
`node/<20.x.y>/<arch>/bin` layout and place an exact-version successful fake for
that token first in `PATH`.

An independent oracle MUST derive the canonical toolcache entry and bounded
launcher/symlink final target from the parsed CI contract, supplied architecture
and observed filesystem without importing production descriptors, expected
sets, branch markers or results. Pre-import audit/profile and process observers
MUST prove the actual selected executable target/argv equals that oracle target,
the hosted branch is traversed and the fake is not invoked. Local
`_SYSTEM_ORIGINS`, live-PATH selection, a successful usability probe or a
production-declared hosted marker MUST NOT satisfy any hosted token case.

Missing, relative, symlinked or outside-root toolcache; wrong/duplicate
version, architecture or token; zero/multiple targets; broken, absolute,
traversing or escaping launcher links; fake-first selection; or hosted-to-system
fallback MUST return bounded non-authoritative aggregate failure with
`semantic_started:0`. External hooks and before/after snapshots MUST show zero
later Git, scheduler, write-intent and mutation events.

#### Scenario: Every canonical hosted token is actually selected
- **WHEN** source-safe focused proof runs the valid `node`, `npm` and `npx` clean-child cases
- **THEN** each case independently observes its exact setup-node toolcache target despite a usable fake-first PATH
- **AND** coverage of the local system-origin branch or three production labels cannot replace any hosted witness.

#### Scenario: Hosted ambiguity cannot fall back to local origin
- **WHEN** any toolcache root, version, architecture, token or launcher invariant is absent, ambiguous or unsafe
- **THEN** admission fails before usability, Git, scheduling or mutation rather than selecting `_SYSTEM_ORIGINS` or live PATH
- **AND** affected output remains non-authoritative with externally observed zero later events.

### Requirement: Affected v19 activation proof MUST materialize and compare the observed context-sensitive graph
Future v19 MUST use an independently implemented deterministic source observer
that parses exact affected runner/profile and exact-digest published scheduler/
broker source. It MUST inventory every import, binding, function, predicate,
call and raw sink and execute a finite context-sensitive worklist seeded only
by the exact public affected entrypoint and production argument row, including
exact tasks, jobs and `supervisor=None`.

For every syntactic item the observer MUST materialize a canonical row
containing stable item ID/kind, repository-relative source path and digest,
qualified owner, canonical AST field/index path, normalized bound-argument
context and predicate facts, exact finite callee/receiver set where applicable,
reachability and exact unreachable reason or raw-sink classification. It MUST
derive these rows only from parsed source and seed arguments and MUST NOT import,
generate from or complete them with `ACTIVATION_CATALOG` or production
registries.

The independently authored immutable `ACTIVATION_CATALOG` and the full observed
row multiset MUST compare bidirectionally by identity and every field. A
length/uniqueness-only check, extra observed row, unused catalog row, self-
derived row, changed context/argument/predicate/callee, unknown/empty/ambiguous
binding, runtime rebind, dynamic wrapper, unlisted sink or latent-to-reachable
transition MUST fail closed.

#### Scenario: Catalog cardinality cannot substitute an observed graph
- **WHEN** catalog rows are unique and have the expected count but no source-derived observed rows exist or one field/set differs
- **THEN** bidirectional graph equality fails before behavioral evidence is admitted
- **AND** neither catalog construction nor production metadata can manufacture the missing observation.

### Requirement: Affected v19 reachable activation MUST have an external dynamic witness
Future v19 MUST run a separate clean child with profile/audit hooks installed
before public runner import. The child MUST invoke exact public affected
activation with the admitted production argument row and externally observe
qualified calls, scheduler arguments and raw process sinks without replacing
production functions, constants, calls or results.

After explicit proof-harness/interpreter exclusions, the dynamic reachable
edge/sink projection MUST equal the static observer's reachable projection.
The exact path MUST enter affected runner/profile, call published scheduler
with `supervisor=None`, select only the published default broker and reach only
cataloged broker sinks. The published injected-supervisor path MUST remain in
syntax, observed rows and catalog with an exact argument/predicate-backed
unreachable reason. Trace absence alone MUST NOT prove unreachability; every
reachable static row requires a dynamic witness and every latent row requires
the observer-derived reason.

#### Scenario: Default activation and latent injection remain distinguished
- **WHEN** focused proof activates the exact public affected entrypoint
- **THEN** static and dynamic reachable projections match through the scheduler default and broker sinks
- **AND** a non-None supervisor, alternate wrapper/sink, missing reachable witness or latent row without a predicate-backed reason fails closed.

### Requirement: Affected v19 investigation MUST preserve the floor and freeze one clean successor order
Future v19 MUST preserve the published authorization v18 proof inventory and
digests, exact 35-to-30 typed registry, independently authored Unicode 16.0.0
`23/235`, aggregate repository/origin/package/runtime/task-root admission
before Git, strict public pure and honest real-Git four-stream selection, typed
scheduler and bounded failures, connected resolved-base/collector/fallback
mutants, full-only publication authority, protocol-artifact non-authority,
closed runner/profile/scheduler/broker ownership, source-safe exact four-step
CI and genuine original-RED chronology.

The only conforming future order MUST be this published investigation,
docs-only `authorize-bounded-affected-release-profile-v19`, clean
`implement-bounded-affected-release-profile-v19`, then
`certify-accelerated-release-loop-v1`. Future authorization MUST contain
exactly:

`{"investigation_card":"openspec/board/4.done/investigate-affected-release-profile-hosted-origin-activation-closure-v19.md","investigation_id":"investigate-affected-release-profile-hosted-origin-activation-closure-v19","successor_card":"openspec/board/3.inprogress/implement-bounded-affected-release-profile-v19.md","successor_id":"implement-bounded-affected-release-profile-v19","production_loc_ceiling":500,"allow_new_authority_or_wire_protocol":true}`.

Future authorization MUST depend exactly on this investigation, the integration
decision, scheduler v1 and authorization v18 and block only implementation v19.
Future implementation MUST use only
`{"authorization_card":"openspec/board/4.done/authorize-bounded-affected-release-profile-v19.md","authorization_id":"authorize-bounded-affected-release-profile-v19"}`,
depend on those four predecessors plus authorization v19, block only
certification, start from authorization-publishing HEAD, retain a new direct
fingerprint-first non-zero missing-module/symbol RED before production/CI/main-
spec mutation and add at most `499` production LOC.

This investigation MUST change only its card, same-slug OpenSpec artifacts,
synchronized release-CI spec and archive metadata. Authorization,
implementation and certification successors MUST remain absent. Reachable
history, real full/affected execution or benchmark, live matrix and
certification checks MUST NOT run. One fresh ordinary `gpt-5.6-sol/high` review
MUST gate publication.

#### Scenario: Investigation leaves one dormant bounded path
- **WHEN** maintainers audit paths, dependencies, successor absence, LOC and verification
- **THEN** executable LOC is zero and only exact authorization v19 can follow this published decision
- **AND** affected output remains non-authoritative while certification stays blocked.
