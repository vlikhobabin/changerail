## 1. Safe boundary and decision contract

- [x] 1.1 Prove local/upstream/remote investigation and remote authorization references equal exact published v18 authorization SHA, and record only validated terminal `9/11`, two-blocker and `1/1/0` summaries.
- [x] 1.2 Verify terminal v18 implementation artifacts/evidence remain excluded and v19 authorization, implementation and certification successors are absent.
- [x] 1.3 Finalize the card, proposal, design and delta specification with exact v19 dependencies, blocks, authorization object, 499 production-LOC successor ceiling and zero executable scope.

## 2. Proof-oracle closure

- [x] 2.1 Freeze separate source-safe clean-child hosted cases for `node`, `npm` and `npx`, independent setup-node/toolcache target derivation, fake-first PATH and fail-closed negative neighbors.
- [x] 2.2 Freeze the source-derived context-sensitive activation worklist, canonical observed row schema, full bidirectional `ACTIVATION_CATALOG` equality and external dynamic reachable witness.
- [x] 2.3 Preserve the published v18 proof inventory/digests, accumulated affected floor, full-only authority and sole investigation → authorization → implementation → certification order.

## 3. Sync, archive and verification

- [x] 3.1 Validate the active change strictly, synchronize its delta into `openspec/specs/changerail-release-ci/spec.md`, verify byte-exact appended requirements and archive the change.
- [x] 3.2 Run strict all-OpenSpec, JSON/TOML parsing, source classification, zero executable LOC, exact relation/successor-absence and `git diff --check` verification.
- [x] 3.3 Run the current-only public-surface scan, confirm manifest scope contains only docs/archive metadata and record that history, real full/affected execution or benchmark, live matrix and certification checks were not run.
