## 1. Clean investigation boundary

- [x] 1.1 Verify exact local/remote authorization v14 base and retain only the
  validated v14 cycle summaries, findings counters and exhausted rescue budget.
- [x] 1.2 Prove v14 is not patched or published and v15 authorization,
  implementation, focused tests and certification remain absent.
- [x] 1.3 Finalize the source-authored origin anchors, pre-import child fixture,
  semantic-mutant uniqueness and exact scheduler/selector discrimination
  decision without executable changes.

## 2. Normative contract

- [x] 2.1 Sync all six v15 delta requirements into
  `openspec/specs/changerail-release-ci/spec.md` idempotently.
- [x] 2.2 Verify the exact six-field future authorization object, successor
  order, dependencies, sole blocks, ceiling and inherited accumulated floor.
- [x] 2.3 Confirm tracked scope adds production/test/runtime LOC `0` and contains
  only the card, same-slug artifacts, synchronized spec and archive metadata.

## 3. Verification

- [x] 3.1 Run strict target and all OpenSpec validation, JSON/TOML parsing,
  source classification and `git diff --check`, including untracked artifacts.
- [x] 3.2 Run current-only `python3 scripts/public-surface-scan.py` and verify
  successor absence; do not run `--history`, full/affected execution,
  benchmarks, live matrix or certification checks.
- [x] 3.3 Reconcile the delivery manifest scope and record why RED is not
  applicable to this docs-only investigation.

## 4. Archive and review handoff

- [x] 4.1 Archive the same-slug change after exact spec sync and rerun strict
  all/whitespace/current-only gates.
- [x] 4.2 Update the card and ignored manifest with reconstructable commands and
  outcomes, leaving the card in `3.inprogress` for fresh ordinary/high review.
