## 1. Decision contract

- [x] 1.1 Verify exact local/upstream/remote base and terminal v17 forensic
  boundary without reading or importing executable v17 artifacts.
- [x] 1.2 Record the sealed catalog, closed mutation language, whitelist call
  graph, connected guard mutant, independent operand parser and external event
  observation decisions in the card and same-slug OpenSpec artifacts.
- [x] 1.3 Freeze exact authorization/implementation v18 lineage, dependency
  sets, sole blocks, ceiling and new original-RED requirement while successors
  remain absent.

## 2. Spec synchronization and archive

- [x] 2.1 Sync the `changerail-release-ci` delta into the main spec without
  changing production, tests, CI or runtime surfaces.
- [x] 2.2 Run `bin/openspec validate investigate-affected-release-profile-proof-oracle-closure-v18 --strict`,
  `bin/openspec validate --all --strict` and archive the same-slug change.
- [x] 2.3 Update the card to `3.inprogress` with exact archive path, concise
  observed verification outcomes and docs-only RED-not-applicable statement.

## 3. Bounded verification

- [x] 3.1 Parse project JSON/TOML, prove exact source classification and zero
  production/test/runtime LOC, and confirm authorization/implementation/
  certification v18 successors are absent.
- [x] 3.2 Run `python3 scripts/public-surface-scan.py` current-only,
  `git diff --check`, delivery-manifest scope reconciliation and deterministic
  ordinary/high preflight; do not run history, release/affected execution,
  benchmark, live matrix or certification checks.
