## 1. Investigation contract

- [x] 1.1 Зафиксировать exact published base, terminal v16 chronology,
  forensic-only boundary и zero-executable scope.
- [x] 1.2 Опубликовать split selector contract: complete pure `R/C 000..100`
  grammar отдельно от honest real-Git non-zero emission fixtures.
- [x] 1.3 Опубликовать independent case-to-guard scheduler relation и bounded
  canonical AST operator/operand mutation language.

## 2. Lineage and accumulated floor

- [x] 2.1 Зафиксировать exact future authorization object, dependencies,
  successor ceiling `500`, implementation ceiling `499` и sole certification
  block.
- [x] 2.2 Сохранить typed registry/admission, independent ledgers, closed
  execution graph, Unicode, full-only authority, protocol non-authority,
  four-step CI и original-RED floor без executable successor.

## 3. Sync, archive and verification

- [x] 3.1 Синхронизировать same-slug delta в
  `openspec/specs/changerail-release-ci/spec.md` byte-exactly и архивировать
  change.
- [x] 3.2 Выполнить strict OpenSpec, JSON/TOML, source classification,
  current-only public scan, successor absence, whitespace и manifest scope;
  подтвердить production/test/runtime LOC `0` и не запускать prohibited checks.
- [x] 3.3 Подготовить card/manifest для fresh ordinary
  `gpt-5.6-sol/high` review и scoped publish.
