## 1. Exact authorization boundary

- [x] 1.1 Prove local/upstream/remote authorization and remote investigation refs equal exact published investigation v19 SHA and terminal v18 remains excluded.
- [x] 1.2 Verify the exact six-field object, four dependencies, sole implementation block, future two-field reference, five implementation dependencies and 499 LOC maximum.
- [x] 1.3 Verify implementation v19 and certification successors remain absent and authorization production/test/runtime LOC is zero.

## 2. Preserved proof floor

- [x] 2.1 Bind unchanged v18 proof inventory counts `35/30/48`, both exact digests, total ownership and static migration without copying or regenerating it.
- [x] 2.2 Freeze separate hosted `node`/`npm`/`npx` witnesses, independent setup-node target derivation, fake-first PATH and complete negative matrix.
- [x] 2.3 Freeze independently materialized activation rows, full bidirectional catalog equality, external dynamic reachable projection and predicate-backed latent injection.
- [x] 2.4 Preserve genuine original RED chronology, accumulated affected floor, full-only authority and protocol-artifact non-authority.

## 3. Sync, archive and verification

- [x] 3.1 Validate strictly, synchronize the delta byte-exactly into the main release-CI spec and archive the change.
- [x] 3.2 Run strict all-OpenSpec, JSON/TOML, source classification, exact relations/inventory/successor absence, current-only public scan, whitespace and manifest scope checks; record prohibited checks as not run.
