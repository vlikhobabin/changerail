## Why

Published investigation v22 closed actual-execution binding, row-complete
activation topology and runtime-ancestry design gaps, but executable v22
remains forbidden without a separate clean, reviewed and remotely published
authorization.

## What Changes

- Fix the exact six-field authorization object and sole bounded implementation
  v22 successor with source ceiling `500` and effective maximum `499` added
  production LOC.
- Preserve legacy scheduler/broker compatibility while authorizing only one
  additive sealed admitted-execution transport and atomic descriptor-bound
  target exec.
- Carry the complete public-seeded row worklist, immutable activation catalog,
  raw-event mapping and exact normalized static/catalog/dynamic topology proof.
- Carry the directory-FD anchored ancestry proof and new disposable original-
  RED episode with no workspace fallback diagnostics.
- Preserve immutable v18 inventory, accumulated selector/hosted/CI guards,
  full-only authority and affected/protocol non-authority.
- Keep authorization docs-only and dormant; create no implementation or
  certification surface.

## Capabilities

### New Capabilities

- none.

### Modified Capabilities

- `changerail-release-ci`: add the exact v22 authorization boundary for one
  clean implementation while preserving the accumulated release-CI floor.

## Impact

The change affects one board card, same-slug OpenSpec artifacts, the
synchronized release-CI specification and archive metadata. Production,
tests, CI/runtime behavior and publication authority do not change; terminal
v21 payload/evidence remains forensic-only.
