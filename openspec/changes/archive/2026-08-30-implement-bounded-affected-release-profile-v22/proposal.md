## Why

Published authorization v22 permits exactly one clean implementation to turn
the dormant scheduler into a bounded affected feedback path while closing the
remaining actual-execution, activation-topology and runtime-ancestry gaps.

## What Changes

- Add the public release profile and strict four-stream affected selector while
  preserving exact full-release behavior and full-only authority.
- Add one closed admitted-execution transport from scheduler to broker with
  sealed descriptor-bound direct and sequential-group execution.
- Add focused independent proofs for immutable inventory parity, actual exec,
  full activation topology, anchored ancestry, hosted tools and exact CI.
- Wire canonical CI to one explicit full-release entrypoint.
- Preserve legacy scheduler/broker APIs and all accumulated fail-closed floors.

## Capabilities

### New Capabilities

- none.

### Modified Capabilities

- `changerail-release-ci`: implement the authorized bounded affected profile,
  admitted execution and non-authoritative developer-feedback contract.

## Impact

The change adds at most 499 production LOC across release profile/admission
code and compatible scheduler/broker/CI wiring, plus one focused proof and the
card-owned synchronized specification. No dependency, receipt, review/publish
authority or certification artifact is added.
