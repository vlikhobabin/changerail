## ADDED Requirements

### Requirement: Affected v22 implementation MUST retain one clean authorized episode
`implement-bounded-affected-release-profile-v22` MUST start from exact
published authorization v22 HEAD
`d33dab914766b1070d296d5bc69076d259386c15`, use only
`{"authorization_card":"openspec/board/4.done/authorize-bounded-affected-release-profile-v22.md","authorization_id":"authorize-bounded-affected-release-profile-v22"}`,
depend exactly on the five card predecessors, block only certification and add
at most `499` production LOC.

Before production, CI or main-spec mutation it MUST retain one direct
fingerprint-first non-zero evidence capture with a concrete absent production
module or symbol and a reachable reconstructable saved tree containing only
the card, same-slug OpenSpec and focused test. Terminal v21 payload/evidence,
late reproduction, zero-exit wrappers and workspace fallback execution MUST
not satisfy this chronology.

#### Scenario: Original RED is reconstructable and clean
- **WHEN** implementation chronology is reviewed against authorization HEAD
- **THEN** the retained failed capture exposes its tree SHA, diff fingerprint and missing symbol
- **AND** that tree contains no production, CI or main-spec mutation.

### Requirement: Affected v22 implementation MUST preserve exact inventory and selection
Implementation MUST use unchanged published authorization v18 proof inventory
as the sole immutable external anchor and preserve exact `35/30/48`, semantic
digest `7147ee3c4b067486162f3dc1fee218c87eb40cbdb0d7730a9a78442da7986513`,
full digest `6587ad0b9887e79f731cdf1ef25f7ff139140747ac9f4def3aeda762c1c4ae72`,
total 35→30 ownership and `36 - 4 - (3 - 1) = 30`. It MUST NOT create a v22
inventory copy or use the proof artifact at runtime.

Affected selection MUST aggregate strict bounded committed, staged, unstaged
and untracked NUL streams, retain rename/copy old and new operands and map only
exact owned paths. Invalid base/status/framing/path, unknown or ambiguous
ownership, self-change, Git fault or bound breach MUST select the exact full
inventory with a deterministic reason. Unicode 16.0.0 `23/235` and all
aggregate repository/origin/package/runtime/task-root guards remain closed.

#### Scenario: Uncertainty falls back without authority
- **WHEN** any selector or aggregate admission input is unknown, ambiguous, malformed, stale or over bound
- **THEN** exact full semantic and physical inventory is selected or semantic launch remains zero
- **AND** requested affected remains `authoritative:false`.

### Requirement: Affected v22 implementation MUST bind admitted rows to atomic FD exec
Legacy scheduler `run_plan` and broker `supervise` MUST remain compatible.
Exactly one additive `changerail.release-admitted-execution.v1` transport MUST
bind each ordered physical owner to one direct member or one outer executor and
all ordered inner members. Every record MUST bind IDs, logical and deterministic
physical argv/FD map, executable/operand ancestry, closed environment and
canonical digests, with one physical-row digest over all member digests.

Before worker start the scheduler MUST reject every missing, extra, duplicate,
reordered, cross-owner, malformed, partial, stale or mismatched record. It MUST
open executable/operand objects no-follow, verify identities and seal the full
nested table in a bounded memfd. The additive broker MUST atomically execute
the verified native executable FD through FD-capable `os.execve`, use a CLOEXEC
status pipe and never path-based Popen. Script/launcher operands MUST be pinned
inherited `/proc/self/fd/<n>` references. Composite work stays one result and
first failure starts zero later members.

#### Scenario: Ambient or stale execution cannot pass admission
- **WHEN** environment, row, bundle or component changes, a fake precedes PATH, a target swaps before open or its name changes after open
- **THEN** the admitted FD object and exact closed row executes or failure occurs before an unadmitted start
- **AND** legacy Popen, raw-token fallback and passing protocol output cannot satisfy admission.

### Requirement: Affected v22 implementation MUST prove actual execution independently
Focused proof MUST derive every direct/outer/inner expected row from the
published inventory, pinned source-safe CI and externally built disposable
filesystem, never production descriptors, `_SYSTEM_ORIGINS`, live PATH,
usability-only output or a production marker. It MUST observe exact exec input,
FD identities, sealed bundle bytes/seals and post-exec `/proc` identity and
compare every logical/physical argv, environment, ancestry and digest field
bidirectionally.

The negative matrix MUST reject missing/extra/reordered/cross-owner records,
changed environment/bundle, fake-first selection, unsafe launcher, pre-open
swap and malformed neighbors before target start. Post-open rename MUST still
execute the opened object and admitted work MUST reach zero legacy Popen or
raw-token execution calls.

#### Scenario: Production cannot attest its own target
- **WHEN** production admission metadata and an execution marker drift together
- **THEN** independent FD, sealed-bundle, exec-input and post-exec comparisons fail
- **AND** only published inventory, pinned CI and external filesystem define expected truth.

### Requirement: Affected v22 implementation MUST prove every activation row
Focused proof MUST independently parse exact runner/profile/group/admission and
exact-digest scheduler/broker sources into every import, binding, function,
predicate, call and raw sink. One finite context-sensitive worklist MUST start
only at public `run_profile("affected", base=<known-docs-base>, jobs=1,
environment=<production-shaped>)` and annotate every row with source/digest,
owner, AST path/span, kind, normalized context/predicates, finite callee set,
predecessor/transfer, reachability/reason and sink class.

A separately authored immutable `ACTIVATION_CATALOG` MUST equal the complete
observed annotated multiset bidirectionally by every field. Empty/sentinel,
self-derived, unknown/ambiguous, extra/missing, rebind, alternate sink or latent
transition state MUST fail before dynamic proof.

#### Scenario: Counts cannot replace row-complete proof
- **WHEN** one syntax row loses an annotation/predecessor or changes reachability/binding
- **THEN** full observed/catalog equality or predecessor closure fails
- **AND** counts, digests and sentinel reasons cannot authorize activation.

### Requirement: Affected v22 implementation MUST prove exact dynamic topology
A disposable pre-import clean child MUST install test-only `sitecustomize`, a
nonce-bound bounded Unix datagram collector, import guard, opcode/profile/audit
hooks and at-fork registration before production import/spawn/exec. Closed
registration/events MUST retain complete roles, parentage, sequence, source,
caller/span and normalized context without observer row IDs.

Every raw production occurrence MUST map uniquely to one catalog row. Only
identical timing-loop activation keys MAY collapse while retaining bounded raw
counts. Exact normalized dynamic topology, static reachable projection and
catalog reachable projection MUST compare bidirectionally and connect public
affected through admitted scheduler, descriptor-bound broker exec, sealed
group executor and ordered inner broker calls. Registration, topology, digest,
transport, mapping, bounds or deadline drift MUST fail closed.

#### Scenario: Repeated polling cannot hide a topology edge
- **WHEN** one exact timing callsite repeats a different bounded count
- **THEN** its raw occurrences normalize only under one identical canonical key
- **AND** any changed role, row, context, predicate, callee or sink remains an extra failure.

### Requirement: Affected v22 implementation MUST anchor runtime ancestry before semantics
Runtime root MUST be an absolute lexical direct child of one exact real anchor
with lexical/real equality. Admission MUST open the anchor and walk every
component through directory-FD `O_DIRECTORY|O_NOFOLLOW|O_CLOEXEC`, compare
device/inode/type/mount identities, reject repeats, symlinks, aliases, mounts,
outside transitions, non-direct children, unknown neighbors and non-empty
targets, then recheck the exact chain immediately before reservation.

Every ancestry failure MUST remain bounded and non-authoritative with
`semantic_started:0` and externally observed zero later process, Git,
scheduler, write or mutation events.

#### Scenario: Alias fails before reservation
- **WHEN** any lexical ancestor resolves through a symlink, alias, repeated identity, mount or outside transition
- **THEN** admission fails before scheduler reservation
- **AND** no Git, semantic or later mutation event starts.

### Requirement: Affected v22 implementation MUST preserve hosted CI and sole authority
Focused proof MUST independently parse the exact pinned source-safe four-step
CI, derive canonical hosted `node`, `npm` and `npx` targets from
`RUNNER_TOOL_CACHE/node/<20.x.y>/<arch>/bin`, resolve launchers within bounds
and prove usable fake-first PATH is not selected with zero hosted-to-system
fallback. Missing/relative/symlinked/outside-root, wrong/duplicate
version/architecture/token, unsafe launcher and zero/multiple targets MUST
fail before semantic launch.

Canonical CI MUST contain the exact checkout, setup-node, install and explicit
full-release steps in order and no duplicate, wrapped, chained, indirect,
inactive or alternate execution surface. Zero arguments and explicit
`full-release` are identical. Affected always returns `authoritative:false`,
including exact-full fallback. Only admitted successful requested full-release
over exact full inventory MAY return `authoritative:true`; protocol and proof
artifacts have no authority.

#### Scenario: Affected success cannot authorize publication
- **WHEN** requested affected passes a subset or its exact-full fallback
- **THEN** its result and every protocol/proof artifact remain non-authoritative
- **AND** review, publish, receipt and certification reject them as full-release evidence.

### Requirement: Affected v22 implementation MUST remain bounded before certification
Implementation proof MUST use only disposable real-Git fixtures and an exact
harmless command allowlist with retained process/event evidence. Workspace
diagnostics MUST NOT invoke public affected or exact-full fallback. Delivery
MUST run focused/static/current checks, strict OpenSpec, JSON/TOML, source
classification, current-only public scan, whitespace and manifest scope.

It MUST NOT run or accept terminal v21 payload/evidence, reachable history,
real full/affected execution or benchmark, live matrix or certification checks.
The card-owned delta MUST synchronize to the main release-CI spec and archive
before one fresh ordinary `gpt-5.6-sol/high` review and scoped publication.

#### Scenario: Implementation cannot self-certify
- **WHEN** implementation verification completes
- **THEN** only allowed focused/static/current evidence gates review
- **AND** certification remains the sole later owner of history, full baseline and timing evidence.
