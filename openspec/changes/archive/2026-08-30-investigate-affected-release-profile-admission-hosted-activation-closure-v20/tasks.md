## 1. Safe boundary and decision contract

- [x] 1.1 Prove local/upstream/remote investigation and remote authorization references equal exact published v19 authorization SHA, and retain only validated terminal reviews `6/10`, `7/10`, three-blocker and `1/1/0` summaries.
- [x] 1.2 Verify terminal v19 implementation artifacts/evidence remain excluded and v20 authorization, implementation and certification successors are absent.
- [x] 1.3 Finalize the card, proposal, design and delta specification with exact v20 dependencies, blocks, authorization object, 499 production-LOC successor ceiling and zero executable scope.

## 2. Proof architecture closure

- [x] 2.1 Freeze the process-free Phase-A aggregate barrier, bounded Phase-B probes, independently authored stage catalog and externally observed zero-event negative neighbors.
- [x] 2.2 Freeze independently parsed CI/setup-node major/layout, component-safe hosted containment, bounded npm/npx launcher resolution and separate fake-first `node`/`npm`/`npx` witnesses.
- [x] 2.3 Freeze one source-derived context-sensitive worklist for row reachability, predecessor/transfer/owner closure, row-derived static projection, complete catalog equality and exact-row dynamic witnesses.
- [x] 2.4 Preserve the published v18 proof inventory/digests, accumulated affected floor, full-only authority and sole investigation → authorization → implementation → certification order.

## 3. Sync, archive and verification

- [x] 3.1 Validate the active change strictly, synchronize its delta into `openspec/specs/changerail-release-ci/spec.md`, verify byte-exact appended requirements and archive the change.
- [x] 3.2 Run strict all-OpenSpec, JSON/TOML parsing, source classification, zero executable LOC, exact relation/successor-absence and `git diff --check` verification.
- [x] 3.3 Run the current-only public-surface scan, confirm manifest scope contains only docs/archive metadata and record that history, real full/affected execution or benchmark, live matrix and certification checks were not run.
