## ADDED Requirements

### Requirement: Affected v21 investigation MUST establish clean terminal lineage
The v21 investigation MUST start from published authorization v20 HEAD
`e04dc38582490339d55876acc512623a0547a2ec`. It MUST carry only the validated
terminal v20 review counters `5/11`, findings `6/0/0`, six finding classes and
published-source compatibility analysis. Terminal v20 card, OpenSpec, source,
tests, CI/main-spec mutation, manifest, verdicts, logs and raw evidence MUST
remain forensic-only after handoff.

The decision MUST modify only its card, same-slug OpenSpec artifacts,
synchronized release-CI spec and archive metadata. Production, test and runtime
LOC MUST remain zero. It MUST NOT create v21 authorization, implementation or
certification, and MUST NOT run history, real full/affected execution or
benchmark, live matrix or certification checks.

#### Scenario: Incompatible same-card repair expands the series safely
- **WHEN** maintainers inspect terminal counters, published scheduler schema and immutable sequential group
- **THEN** the unused same-card repair is not spent on an implementation that cannot satisfy the exact verification target
- **AND** the clean v21 investigation is the only new series stage.

### Requirement: Affected v21 MUST preserve one result and every composite argv identity
Future v21 MUST retain the immutable v18 `35/30/48` inventory, both published
digests, total 35→30 ownership and `36 - 4 - (3 - 1) = 30`. Direct tasks MUST
present their exact immutable argv to scheduler v1. A sequential-group owner
MUST remain one scheduler task/result whose separately typed repository
group-executor transport contains only owner ID and expected production registry
digest.

The group executor MUST import the production registry, validate exact digest,
owner, kind and complete ordered group, and invoke published broker v5 once for
each immutable argv in order with bounded timeouts. It MUST accept only exact
closed passing broker tuples. First failure, timeout, cleanup fault, malformed
result or identity mismatch MUST stop later commands and make the one outer
physical result fail. Direct subprocess/shell execution, raw proof-inventory
input, wrapper argv substitution, skip, duplicate or reorder MUST fail closed.

#### Scenario: Nested broker preserves immutable group identity
- **WHEN** an independent oracle observes the outer scheduler/broker transport and the inner group execution
- **THEN** scheduler returns one row for the physical owner and inner broker observations equal all three immutable argv in exact order
- **AND** an injected failure at each index starts no later argv and leaves no owned survivor.

### Requirement: Affected v21 MUST validate the full scheduler envelope locally
Future profile pass and authority MUST require an exact closed scheduler summary:
published version, exact requested jobs, exact top-level fields, exact status and
one result per selected physical owner in registry order. Every result MUST have
the exact published field set, exact selected owner ID and internally consistent
passing tuple. Selected semantic ownership and production registry digest MUST
also match before pass.

Missing, empty, duplicate, unknown, reordered, extra, malformed, failing or
cross-owner results and wrong version/jobs/status/digest MUST fail locally and
MUST NOT manufacture full-release authority. Scheduler/protocol artifacts remain
authority-free and requested affected remains `authoritative:false`.

#### Scenario: Forged result tables cannot authorize release
- **WHEN** focused mutants substitute every malformed top-level or result-row neighbor
- **THEN** each neighbor returns bounded failure and full authority remains false
- **AND** only the exact complete passing table for the requested plan is admitted.

### Requirement: Affected v21 activation proof MUST equal exact public callsites
An independent observer MUST parse exact runner/profile/group-executor and
exact-digest scheduler/broker sources into the complete import, binding,
function, predicate, call and raw-sink row multiset. One finite
context-sensitive worklist MUST start only at public
`run_profile("affected", base=<known-docs-base>, jobs=1,
environment=<production-shaped>)` and assign every row canonical source/digest,
owner, AST path/span, context/predicates, callee/receiver, predecessor/transfer,
reachability/reason and sink class. Internal `main(..., supervisor=None)` MUST be
reachable only through that public path.

A separate pre-import clean child MUST run the exact public call in a disposable
real-Git fixture with harmless exact selected commands and production
`supervisor=None`. Caller code identity and bytecode positions MUST map every
qualified Python/C call and raw sink uniquely to canonical AST row IDs. The
complete dynamic occurrence multiset MUST equal the static row-derived reachable
call/sink projection bidirectionally through scheduler default, outer broker,
group executor and inner brokers. Unknown, ambiguous, zero/multiple, missing or
extra mappings MUST fail. Non-None supervisor MUST remain cataloged and
predicate-backed unreachable; trace absence MUST NOT prove it.

Before that child starts, the harness MUST bind a test-only bounded Unix
datagram collector and materialize `sitecustomize.py` with a fresh nonce, hard
deadline, exact source-digest catalog and finite datagram/event/byte bounds.
Inherited `PYTHONPATH` MUST install one registration, production-import guard,
opcode/profile/audit hooks and at-fork child registration before production
import in the public interpreter, every scheduler `spawn` interpreter and the
execed group executor. Registration/event datagrams MUST use a closed schema
with nonce, pid/ppid, bounded role and monotonic per-process sequence and MUST
carry caller code/span inputs rather than observer-authored row IDs.

The collector MUST reconcile every registered interpreter/fork child with
observed spawn/exec/fork topology before row equality. Event-before-registration,
missing, late, duplicate or bad-nonce registration, sequence gap/replay,
unknown production-importing role, disconnected parentage, unexpected
interpreter, digest mismatch, malformed/oversized datagram, overflow, timeout
or collector loss MUST fail. Connected mutants MUST remove/delay bootstrap for
the public child, one scheduler spawn and executor exec and inject duplicate,
bad-nonce and disconnected registration. Test-only bootstrap/transport MUST NOT
be read by production or count as a row, pass, authority or unreachable proof.

#### Scenario: Public activation cannot be replaced by internal reachability
- **WHEN** seed, binding, predicate, callsite, wrapper, sink, mapping or dynamic occurrence is removed, added or redirected
- **THEN** full catalog, owner closure or exact static/dynamic row equality fails
- **AND** function-entry counts, sink classes, selected edges, hashes or absent trace cannot substitute exact callsite identity.

### Requirement: Affected v21 MUST close runtime hosted and CI guard neighbors
Phase A MUST accept an existing runtime root only when it is a real directory
with zero entries. Any known or unknown child, dangling entry, symlink, alias,
non-directory or outside-root neighbor MUST fail before every process and later
Git/scheduler/write/mutation event with `semantic_started:0` and
`authoritative:false`.

Hosted npm/npx launcher text MUST equal exact relative
`../lib/node_modules/npm/bin/{npm|npx}-cli.js` and resolve through real
non-symlinked ancestors to the canonical target. Obfuscated traversal,
alternate spelling and every v20 hosted negative neighbor MUST fail before
process without system fallback.

Canonical CI MUST parse as the exact pinned four-step YAML object. The
dependency block MUST equal its exact three commands and the final scalar MUST
be the only exact full-release invocation in every scalar/block or
execution-bearing field. Duplicate, wrapped, chained, indirect, inactive,
reordered or alternate semantics MUST fail.

#### Scenario: Closed neighbors reject before semantic launch
- **WHEN** focused connected cases add any runtime entry, alternate hosted launcher spelling or second full-release occurrence
- **THEN** runtime/hosted cases record zero process and later events while CI structure fails deterministically
- **AND** no usability result, resolved target or hidden block command can satisfy the guard.

### Requirement: Affected v21 investigation MUST freeze exact successor bounds
Future authorization MUST depend exactly on this investigation, the integration
decision, scheduler v1 and authorization v20 and block only implementation v21.
Future implementation MUST depend exactly on those four predecessors plus
authorization v21, block only certification, start from the authorization-
publishing HEAD, use only its exact two-field authorization reference, retain a
new genuine fingerprint-first original RED and add at most `499` production
LOC.

The successor MUST preserve Unicode 16.0.0 `23/235`, strict committed/staged/
unstaged/untracked selection with rename/copy old/new operands, deterministic
exact-full fallback, full-only publication authority, affected/protocol
non-authority and source-safe four-step CI. Published v18 proof inventory MUST
remain the sole immutable external anchor and MUST NOT become runtime input.

#### Scenario: Authorization and implementation remain absent during decision
- **WHEN** this investigation is reviewed for publication
- **THEN** exact future relations, authorization object, ceiling and accumulated floor are machine-checkable
- **AND** successor cards/code and certification evidence remain absent.
