---
name: chrl-deliver
description: Short alias for changerail-deliver. Use when the user asks for `$chrl-deliver`, `chrl:deliver` or `/chrl:deliver`; follow the canonical supervised ChangeRail delivery pipeline contract.
---

# chrl-deliver

`chrl-deliver` is an official short alias for `changerail-deliver`.

Read `../changerail-deliver/SKILL.md` completely and follow that canonical
contract. Treat `$chrl-deliver ...`, `chrl:deliver ...` and
`/chrl:deliver ...` as equivalent invocation forms for
`$changerail-deliver ...`.

Do not introduce separate `chrl` runtime paths, schema ids or OpenSpec
namespaces; `chrl` is invocation shorthand only.
