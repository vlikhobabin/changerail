---
name: chrl-do
description: Short alias for changerail-do. Use when the user asks for `$chrl-do`, `chrl:do` or `/chrl:do`; follow the canonical ChangeRail delivery contract.
---

# chrl-do

`chrl-do` is an official short alias for `changerail-do`.

Read `../changerail-do/SKILL.md` completely and follow that canonical contract.
Treat `$chrl-do ...`, `chrl:do ...` and `/chrl:do ...` as equivalent invocation
forms for `$changerail-do ...`.

Do not introduce separate `chrl` runtime paths, schema ids or OpenSpec
namespaces; `chrl` is invocation shorthand only.
