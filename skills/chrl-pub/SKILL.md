---
name: chrl-pub
description: Short alias for changerail-pub. Use when the user asks for `$chrl-pub`, `chrl:pub` or `/chrl:pub`; follow the canonical ChangeRail publish contract.
---

# chrl-pub

`chrl-pub` is an official short alias for `changerail-pub`.

Read `../changerail-pub/SKILL.md` completely and follow that canonical contract.
Treat `$chrl-pub ...`, `chrl:pub ...` and `/chrl:pub ...` as equivalent
invocation forms for `$changerail-pub ...`.

Do not introduce separate `chrl` runtime paths, schema ids or OpenSpec
namespaces; `chrl` is invocation shorthand only.
